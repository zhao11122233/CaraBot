# CaraBot Agent 智能知识库系统课程
## 第 12 课：文档处理流水线

### 🎯 本节课目标
- 理解 RAG 中文档处理流水线的完整流程
- 掌握多格式文档解析（PDF、DOCX、Markdown、TXT）
- 深入分析 RecursiveCharacterTextSplitter 的分块策略
- 学习通过 MIME 类型和扩展名双重检测来路由不同的 Loader
- 学习时长：2 天
- 实践任务：调试文档分块过程

---

## 1. 文档处理流水线全景

### 1.1 流程图

```
上传文件 (bytes + filename + mime_type)
  │
  ▼
① 格式检测
  ├── MIME type 已知 → 直接用
  └── MIME type 未知 → 从扩展名推断
  │
  ▼
② 文件大小校验
  └── > max_file_size → 拒绝（413 FileTooLargeException）
  │
  ▼
③ 选择 Loader
  ├── PDF       → PyPDFLoader
  ├── DOCX/DOC  → Docx2txtLoader
  ├── Markdown  → UnstructuredMarkdownLoader → 后备 TextLoader
  └── TXT       → TextLoader
  │
  ▼
④ 加载文档 → list[Document]（每页/每段一个 Document）
  │
  ▼
⑤ 文本分块（RecursiveCharacterTextSplitter）
  └── 按优先级分隔符递归切分：\n\n → \n → 。 → . → " " → ""
  │
  ▼
⑥ 注入元数据（source, mime_type, document_id, author...）
  │
  ▼
⑦ 返回 list[Document] chunks
```

### 1.2 数据流

```
输入：bytes + "report.pdf" + "application/pdf"
  → 20 页 PDF
  → 每页一个 Document = 20 个 Documents
  → 切块后 = 85 个 chunks（每个 ≤ 500 字符）
  → 每个 chunk 嵌入为 1024 维向量
  → 存入向量库
```

---

## 2. 多格式文档加载

### 2.1 支持的格式

```python
SUPPORTED_MIMETYPES = {
    "application/pdf": ".pdf",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document": ".docx",
    "application/msword": ".doc",
    "text/markdown": ".md",
    "text/plain": ".txt",
    "text/x-markdown": ".md",
}

SUPPORTED_EXTENSIONS = {".pdf", ".docx", ".doc", ".md", ".txt", ".markdown"}
```

### 2.2 Loader 路由逻辑

```python
def _load_file(self, file_path: str, mime_type: str) -> list[Document]:
    if mime_type == "application/pdf":
        from langchain_community.document_loaders import PyPDFLoader
        loader = PyPDFLoader(file_path)
        return loader.load()

    elif mime_type in (
        "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
        "application/msword",
    ):
        from langchain_community.document_loaders import Docx2txtLoader
        loader = Docx2txtLoader(file_path)
        return loader.load()

    elif mime_type in ("text/markdown", "text/x-markdown"):
        from langchain_community.document_loaders import UnstructuredMarkdownLoader
        try:
            loader = UnstructuredMarkdownLoader(file_path)
            return loader.load()
        except ImportError:
            # 后备方案：当 unstructured 库不可用时
            from langchain_community.document_loaders import TextLoader
            loader = TextLoader(file_path, encoding="utf-8")
            return loader.load()

    elif mime_type == "text/plain":
        from langchain_community.document_loaders import TextLoader
        loader = TextLoader(file_path, encoding="utf-8")
        return loader.load()

    else:
        raise UnsupportedFileTypeException(f"Cannot load mime type: {mime_type}")
```

### 2.3 各 Loader 的特点

| Loader | 适用格式 | 输出单位 | 注意事项 |
|--------|---------|---------|---------|
| `PyPDFLoader` | PDF | 每页一个 Document | 扫描版 PDF 需要 OCR |
| `Docx2txtLoader` | DOCX/DOC | 整个文档一个 Document | 不保留格式，只提取文本 |
| `UnstructuredMarkdownLoader` | Markdown | 按标题/段落分割 | 需要安装 `unstructured` 库 |
| `TextLoader` | TXT / 后备 | 整个文件一个 Document | 最简单，UTF-8 编码 |

### 2.4 MIME 类型推断：两级检测

```python
def _infer_mime_type(self, filename: str) -> str:
    ext = Path(filename).suffix.lower()
    mapping = {
        ".pdf": "application/pdf",
        ".docx": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
        ".doc": "application/msword",
        ".md": "text/markdown",
        ".markdown": "text/markdown",
        ".txt": "text/plain",
    }
    mime = mapping.get(ext)
    if not mime:
        raise UnsupportedFileTypeException(
            f"Unsupported file extension '{ext}' for file '{filename}'."
        )
    return mime
```

**为什么需要推断？** 因为 HTTP 上传时，某些客户端可能不发送正确的 `Content-Type`（比如 curl 默认用 `application/octet-stream`），此时从文件名扩展名推断。

---

## 3. 文本分块：RecursiveCharacterTextSplitter

### 3.1 为什么需要分块？

- 向量模型有输入长度限制（BGE-m3 最多 8192 tokens）
- 太长的文本嵌入后"语义被稀释"，检索精度下降
- 分块后可以精确检索到相关段落，而不是返回整个文档

### 3.2 RecursiveCharacterTextSplitter 的原理

它用一组**优先级递减的分隔符**递归切分：

```python
splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,         # 目标块大小（字符数）
    chunk_overlap=50,       # 相邻块之间的重叠（字符数）
    separators=["\n\n", "\n", "。", ".", " ", ""],
    # 优先级：段落 → 行 → 中文句号 → 英文句号 → 空格 → 字符
    length_function=len,
)
```

### 3.3 切分过程演示

```
原始文本（1500 字符）：
"第一段内容...\n\n第二段内容...\n\n第三段内容..."

Step 1: 用 "\n\n" 切分
  → ["第一段内容...", "第二段内容...", "第三段内容..."]
  → 第二段 600 字符，超过 chunk_size=500

Step 2: 用 "\n" 切第二段
  → ["第二段内容行1...", "第二段内容行2..."]
  → 每行都在 500 字符以内

Step 3: 添加 overlap
  → Chunk 1: "第一段内容..." + 下一块的前 50 字符
  → Chunk 2: 上一块的后 50 字符 + "第二段内容行1..." + 下一块的前 50 字符
  → Chunk 3: 上一块的后 50 字符 + "第二段内容行2..." + 下一块的前 50 字符
```

### 3.4 chunk_overlap 的作用

```
没有 overlap（overlap=0）：
  Chunk 1: "...RAG 是检索增强生成的缩写..."
  Chunk 2: "...它结合了检索和生成两种技术..."  ← "RAG" 这个词被切断了！

有 overlap（overlap=50）：
  Chunk 1: "...RAG 是检索增强生成的缩写。它结合了检索..."
  Chunk 2: "...是检索增强生成的缩写。它结合了检索和生成两种技术..."
  ↑ 两个 Chunk 共享 50 个字符的上下文，避免关键信息在边界处丢失
```

### 3.5 配置调优

| 参数 | 太小 | 太大 | CaraBot 默认 |
|------|------|------|-------------|
| `chunk_size` | 语义碎片化 | 检索不精确，噪声多 | 500 |
| `chunk_overlap` | 关键信息在边界丢失 | 冗余信息多 | 50 |

---

## 4. DocumentProcessor 完整实现分析

### 4.1 核心方法 load_and_split

```python
async def load_and_split(
    self, file_content: bytes, filename: str, mime_type: str | None = None
) -> list[Document]:
    # ① MIME 类型推断
    if mime_type is None:
        mime_type = self._infer_mime_type(filename)

    # ② 格式校验
    ext = Path(filename).suffix.lower()
    if ext not in SUPPORTED_EXTENSIONS and mime_type not in SUPPORTED_MIMETYPES:
        raise UnsupportedFileTypeException(...)

    # ③ 写临时文件（LangChain Loader 需要文件路径）
    suffix = ext or ".tmp"
    with tempfile.NamedTemporaryFile(suffix=suffix, delete=False) as tmp:
        tmp.write(file_content)
        tmp_path = tmp.name

    try:
        # ④ 加载文档（同步操作，放线程池）
        documents = await asyncio.to_thread(self._load_file, tmp_path, mime_type)
    except Exception as exc:
        raise DocumentException(f"Failed to parse '{filename}': {exc}")
    finally:
        os.unlink(tmp_path)  # ⑤ 清理临时文件

    # ⑥ 注入元数据
    for doc in documents:
        doc.metadata.setdefault("source", filename)
        doc.metadata.setdefault("mime_type", mime_type)

    # ⑦ 分块（同步操作，放线程池）
    chunks = await asyncio.to_thread(self._splitter.split_documents, documents)

    logger.info(
        "Processed '%s': %d document(s) → %d chunks (chunk_size=%d, overlap=%d)",
        filename, len(documents), len(chunks), self._chunk_size, self._chunk_overlap,
    )
    return chunks
```

### 4.2 设计要点

| 设计点 | 实现 |
|--------|------|
| 同步库异步化 | `asyncio.to_thread()` 包装同步 Loader |
| 临时文件管理 | `tempfile.NamedTemporaryFile` + `try-finally` 保证清理 |
| 错误即传播 | Loader 失败 → `DocumentException` |
| 元数据注入 | 每个 chunk 携带 source、mime_type，方便追溯 |

---

## 5. 实践：添加 EPUB 格式支持

### Step 1：添加 MIME 类型

```python
SUPPORTED_MIMETYPES = {
    # ... 已有的
    "application/epub+zip": ".epub",
}
SUPPORTED_EXTENSIONS = {..., ".epub"}
```

### Step 2：添加 Loader 分支

```python
elif mime_type == "application/epub+zip":
    from langchain_community.document_loaders import UnstructuredEPubLoader
    loader = UnstructuredEPubLoader(file_path)
    return loader.load()
```

### Step 3：扩展名映射

```python
def _infer_mime_type(self, filename: str) -> str:
    mapping = {
        # ... 已有的
        ".epub": "application/epub+zip",
    }
```

---

## 6. 课后作业

1. **跟踪一次 load_and_split**：上传一个 PDF 文件，在代码中加日志/断点，观察每一步的输入输出
2. **对比分块参数**：用同一篇长文档，分别用 `chunk_size=200/500/1000` 和 `chunk_overlap=0/50/100` 分块，比较结果差异
3. **添加新格式**：为 CaraBot 添加 CSV 文件支持（提示：使用 `CSVLoader`）
4. **分析切块质量**：上传一篇中英文混合的文档，检查切块结果中是否存在不完整句子的情况
5. **对比 LangChain Splitter**：比较 `RecursiveCharacterTextSplitter` 和 `TokenTextSplitter` 的切块结果差异

---

## 📚 扩展阅读
- LangChain Document Loaders：https://python.langchain.com/docs/integrations/document_loaders/
- LangChain Text Splitters：https://python.langchain.com/docs/modules/data_connection/document_transformers/
- BGE-m3 模型文档：https://huggingface.co/BAAI/bge-m3

---

**下节课预告**：第 13 课 - 文档去重机制（SHA256 哈希、增量更新、软删除）
