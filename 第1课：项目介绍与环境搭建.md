# CaraBot 企业级 RAG 系统课程
## 第 1 课：项目介绍与环境搭建

### 🎯 本节课目标
- 了解 CaraBot 项目的定位和核心特性
- 搭建 Python 3.10+ 开发环境
- 成功克隆项目并安装依赖

---

## 1. 项目介绍

### 1.1 什么是 CaraBot？
CaraBot 是一个企业级多语言 RAG（检索增强生成）知识检索系统，是传音 Carlcare AICC 的核心能力。它可以帮助企业快速搭建智能问答系统、知识库检索平台等 AI 应用。

### 1.2 核心特性
- **多格式文档摄入**：支持 PDF、DOCX、Markdown、TXT 等多种格式
- **多语言嵌入**：使用 BGE-m3 模型，支持中文、英文、斯瓦希里语等多语言
- **双向量存储**：支持 Milvus（生产环境）和 Chroma（开发环境），可通过配置切换
- **文档去重**：基于 SHA256 哈希的去重机制，支持增量更新
- **API Key 鉴权**：所有接口通过 `X-API-Key` 请求头进行认证
- **结构化 JSON 日志**：每个请求带 trace_id，便于分布式追踪
- **健康检查**：组件级监控（数据库、向量存储、模型、Redis）
- **Recall@k 评估**：内置评估工具，支持按语言分析 badcase

### 1.3 应用场景
- 智能客服机器人
- 企业知识库检索
- 文档问答系统
- 内部知识管理平台
- 多语言智能助手

---

## 2. 环境搭建

### 2.1 环境要求
- Python 3.10+（推荐 3.11）
- Docker 和 Docker Compose（用于基础设施部署，可选）
- Git（用于克隆项目）

### 2.2 安装 Python

#### Windows 系统
1. 访问 Python 官方网站：https://www.python.org/downloads/windows/
2. 下载 Python 3.11.x 版本（建议选择 Stable Release）
3. 运行安装程序，勾选 "Add Python to PATH"
4. 选择 "Customize installation"，确保勾选 "pip"
5. 完成安装后，打开命令提示符，输入以下命令验证：
```bash
python --version
pip --version
```

#### macOS 系统
1. 使用 Homebrew 安装：
```bash
brew install python@3.11
```
2. 验证安装：
```bash
python3 --version
pip3 --version
```

#### Linux 系统
1. 使用包管理器安装：
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3.11 python3.11-pip

# CentOS/RHEL
sudo dnf install python3.11 python3.11-pip
```
2. 验证安装：
```bash
python3.11 --version
pip3.11 --version
```

### 2.3 安装 Git
1. 访问 Git 官方网站：https://git-scm.com/downloads
2. 下载对应操作系统的安装包
3. 运行安装程序，使用默认配置即可
4. 验证安装：
```bash
git --version
```

### 2.4 （可选）安装 Docker
1. 访问 Docker 官方网站：https://www.docker.com/products/docker-desktop/
2. 下载对应操作系统的 Docker Desktop
3. 安装并启动 Docker Desktop
4. 验证安装：
```bash
docker --version
docker-compose --version
```

---

## 3. 项目克隆与依赖安装

### 3.1 克隆项目
```bash
# 克隆项目（替换为实际仓库地址）
git clone <仓库地址>
cd CaraBot
```

如果没有 Git 权限，也可以直接下载 ZIP 包并解压。

### 3.2 创建虚拟环境（推荐）
使用虚拟环境可以隔离项目依赖，避免与系统 Python 冲突。

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows（命令提示符）
venv\Scripts\activate.bat

# Windows（PowerShell）
venv\Scripts\Activate.ps1

# macOS/Linux
source venv/bin/activate
```

激活成功后，命令行提示符会显示 `(venv)` 前缀。

### 3.3 安装依赖
```bash
# 升级 pip
python -m pip install --upgrade pip

# 安装项目依赖
pip install -r requirements.txt
```

如果安装速度较慢，可以使用国内镜像源：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 3.4 验证依赖安装
```bash
# 检查关键依赖是否安装成功
pip list | grep -E "fastapi|uvicorn|pymilvus|chromadb|langchain"
```

你应该看到类似以下输出：
```
fastapi               0.115.0
uvicorn               0.32.0
pymilvus              2.4.0
chromadb              0.5.0
langchain             0.3.0
```

---

## 4. 常见问题排查

### 4.1 Python 版本问题
如果系统中安装了多个 Python 版本，可以使用以下方式指定版本：
```bash
# Windows
py -3.11 -m pip install -r requirements.txt

# macOS/Linux
python3.11 -m pip install -r requirements.txt
```

### 4.2 虚拟环境激活问题
- Windows PowerShell 可能需要执行以下命令以允许脚本执行：
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 4.3 依赖安装失败
- 确保已经安装了编译依赖（Linux 系统）：
```bash
# Ubuntu/Debian
sudo apt install build-essential python3.11-dev

# CentOS/RHEL
sudo dnf install gcc gcc-c++ python3.11-devel
```

### 4.4 网络问题
如果无法连接到 PyPI，可以尝试使用代理或更换镜像源：
```bash
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
```

---

## 5. 课后作业
1. 成功安装 Python 3.10+ 和 Git
2. 克隆 CaraBot 项目到本地
3. 创建并激活虚拟环境
4. 成功安装所有项目依赖
5. 记录安装过程中遇到的问题和解决方案

---

## 📚 扩展阅读
- Python 官方文档：https://docs.python.org/3/
- FastAPI 官方文档：https://fastapi.tiangolo.com/
- Milvus 官方文档：https://milvus.io/docs
- Chroma 官方文档：https://docs.trychroma.com/

---

**下节课预告**：第 2 课 - 快速启动与核心功能体验