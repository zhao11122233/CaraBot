# CaraBot Agent 智能知识库系统课程
## 第 25 课：缓存与性能优化

### 🎯 本节课目标
- 掌握 RAG 系统的三个主要性能瓶颈及优化方向
- 学习 Redis 缓存在 CaraBot 中的集成方式
- 理解嵌入缓存、查询结果缓存、会话缓存的区别
- 学会系统性能瓶颈分析方法
- 学习时长：2 天
- 实践任务：添加缓存优化性能

---

## 1. RAG 系统的三个瓶颈

### 1.1 瓶颈分析

```
一次 Agent 对话请求的耗时分布：
┌────────────────────────────────────────────┐
│ ① LLM 推理         ████████████████  60%  │  ← OpenAI API 延迟
│ ② 嵌入模型推理     ██████            20%  │  ← BGE-m3 推理
│ ③ 向量检索         ██                 5%  │  ← Milvus 查询
│ ④ 网络传输          ███              10%  │  ← 各服务间通信
│ ⑤ 数据库操作        █                 5%  │  ← PostgreSQL
└────────────────────────────────────────────┘
```

### 1.2 优化策略

| 瓶颈 | 优化方案 | 预期效果 |
|------|---------|---------|
| LLM 推理 | 流式输出（SSE）、缓存重复问题 | 首字节延迟降低 80% |
| 嵌入推理 | GPU 推理、嵌入缓存（Redis） | 嵌入耗时从 200ms → 2ms（命中） |
| 向量检索 | 更好索引（HNSW）、预热（load） | 检索从 50ms → 2ms |
| 网络传输 | 减少跨服务调用、本地部署 | 网络延迟 → 0 |
| 数据库 | 连接池调优、索引优化 | QPS 提升 2-3 倍 |

---

## 2. 三级缓存策略

### 2.1 嵌入缓存（Redis）

```python
import hashlib, json
import redis.asyncio as aioredis

class CachedEmbeddingService:
    def __init__(self, embedding_service: EmbeddingService, redis_url: str):
        self._embedding = embedding_service
        self._redis = aioredis.from_url(redis_url)

    async def embed_query(self, text: str) -> list[float]:
        # 缓存键：emb:<text的MD5>
        cache_key = f"emb:{hashlib.md5(text.encode()).hexdigest()}"

        # 查缓存
        cached = await self._redis.get(cache_key)
        if cached:
            return json.loads(cached)

        # 缓存未命中 → 计算嵌入
        embedding = await self._embedding.embed_query(text)

        # 写入缓存（1 小时过期）
        await self._redis.set(cache_key, json.dumps(embedding), ex=3600)

        return embedding
```

### 2.2 搜索结果缓存

```python
class CachedSearchService:
    async def search(self, query: str, top_k=5, threshold=0.7):
        # 缓存键：search:<query>:<top_k>:<threshold>
        cache_key = f"search:{query}:{top_k}:{threshold}"

        cached = await self._redis.get(cache_key)
        if cached:
            return json.loads(cached)

        result = await self._search_service.search(query, top_k, threshold)

        # 短时间缓存（5 分钟），适合高频重复查询
        await self._redis.set(cache_key, json.dumps(result), ex=300)

        return result
```

### 2.3 会话级缓存（内存）

```python
# ChatRequest 可以缓存最近的查询结果
class ConversationCache:
    def __init__(self, max_size=100):
        self._cache: dict[str, list] = {}  # thread_id → 最近的结果列表
        self._max_size = max_size

    def get(self, thread_id: str, query: str):
        """检查同一对话中是否问过类似问题"""
        if thread_id in self._cache:
            for cached_query, cached_result in self._cache[thread_id]:
                if self._similar(query, cached_query, threshold=0.95):
                    return cached_result
        return None
```

---

## 3. 连接池优化

### 3.1 数据库连接池

```python
engine = create_async_engine(
    settings.db_url,
    pool_size=10,        # 常驻连接数（根据 QPS 调整）
    max_overflow=20,     # 溢出连接数
    pool_recycle=3600,   # 连接回收时间（秒）
    pool_pre_ping=True,  # 使用前检查连接有效性
)
```

### 3.2 Redis 连接池

```python
redis_client = aioredis.from_url(
    settings.redis_url,
    max_connections=20,
    decode_responses=False,  # 向量数据是二进制
)
```

---

## 4. 性能监控

### 4.1 关键指标

```python
# 在 SearchService 中记录
logger.info(
    "Search perf: embed=%dms, vector=%dms, format=%dms, total=%dms",
    embed_ms, vector_ms, format_ms, total_ms,
)

# 在 AgentService 中记录
logger.info(
    "Agent perf: llm_calls=%d, tool_calls=%d, total=%dms",
    llm_count, tool_count, total_ms,
)
```

### 4.2 慢请求告警

```python
if total_ms > 3000:
    logger.warning(
        "SLOW REQUEST: %s, steps=%d, tools=%d, time=%dms",
        thread_id, steps, tools, total_ms,
    )
```

---

## 5. 课后作业

1. **嵌入缓存**：实现基于 Redis 的嵌入缓存，测量缓存命中率和对搜索耗时的影响
2. **性能基线**：用 Apache Bench 或 wrk 对 `/api/v1/search` 做压力测试，得到 QPS 基线
3. **连接池调优**：分别测试 pool_size=2/5/10/20 下的 QPS，找出最佳配置
4. **慢查询分析**：在日志中找出耗时 > 1 秒的请求，分析耗时分布
5. **内存分析**：用 `memory_profiler` 分析 BGE-m3 模型加载后的内存占用

---

## 📚 扩展阅读
- Redis 缓存模式：https://redis.io/docs/manual/patterns/
- SQLAlchemy 连接池：https://docs.sqlalchemy.org/en/20/core/pooling.html
- Python 性能分析：https://docs.python.org/3/library/profile.html

---

**下节课预告**：第 26 课 - 安全与权限控制
