# CaraBot Agent 智能知识库系统课程
## 第 28 课：容器化部署

### 🎯 本节课目标
- 理解 Docker 多阶段构建的原理和优势
- 深入分析 CaraBot 的 Dockerfile（builder + runtime 分离）
- 掌握 Docker Compose 编排多服务（CaraBot + PostgreSQL + Redis + Milvus）
- 学习时长：2 天
- 实践任务：使用 Docker 部署系统

---

## 1. 多阶段构建

### 1.1 为什么需要多阶段构建？

```
单阶段构建的问题：
  pip install 需要 gcc, make 等编译工具 (~500MB)
  → 这些工具在运行时完全不需要
  → 但它们被打进了镜像，镜像体积巨大

多阶段构建的解决：
  Stage 1 (builder): 安装编译工具 → pip install → 编译完成
  Stage 2 (runtime): 只复制编译好的依赖，不装编译工具
  → 镜像体积减少 50%+
```

### 1.2 CaraBot 的 Dockerfile 详解

```dockerfile
# ===== Stage 1: Builder（编译阶段）=====
FROM python:3.10-slim-bookworm AS builder
WORKDIR /app

# 配置国内镜像源（加速 apt-get）
RUN rm -rf /etc/apt/sources.list.d/* && \
    echo "deb http://mirrors.aliyun.com/debian bookworm main contrib non-free" > /etc/apt/sources.list
RUN apt-get update && apt-get install -y --no-install-recommends gcc g++ make curl

# 安装 Python 依赖到 /install 目录（隔离）
COPY requirements.txt .
RUN pip install --no-cache-dir --prefix=/install -r requirements.txt

# ===== Stage 2: Runtime（运行阶段）=====
FROM python:3.10-slim-bookworm
WORKDIR /app

# 创建非 root 用户（安全）
RUN groupadd -r carabot && useradd -r -g carabot carabot

# 只安装运行时依赖（不需要 gcc/g++/make！）
RUN apt-get update && apt-get install -y --no-install-recommends libgomp1 curl libpq5

# 从 builder 阶段复制已安装的 Python 包
COPY --from=builder /install /usr/local

# 复制应用代码
COPY src/ ./src/
COPY scripts/ ./scripts/

# 创建数据目录
RUN mkdir -p /app/data/models/bge-m3 /app/data/chroma /app/logs \
    && chown -R carabot:carabot /app

# 切换到非 root 用户
USER carabot

EXPOSE 8000

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
    CMD curl -f http://localhost:8000/health || exit 1

CMD ["uvicorn", "src.app.main:create_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]
```

### 1.3 关键设计点

| 设计 | 为什么 |
|------|--------|
| `--prefix=/install` | 将 pip 包安装到独立目录，方便复制到 runtime 阶段 |
| `COPY --from=builder` | 只复制编译产物，不复制编译工具 |
| `USER carabot` | 非 root 运行（安全最佳实践） |
| `HEALTHCHECK` | Docker 能自动检测容器健康状态并重启不健康的容器 |
| `python:3.10-slim-bookworm` | 最小化基础镜像（~50MB vs 完整版 ~900MB） |
| `libgomp1` 运行时依赖 | BGE-m3 / sentence-transformers 需要 OpenMP 运行时 |

---

## 2. Docker Compose 编排

### 2.1 服务拓扑

```
┌──────────────────────────────────────────────┐
│              Docker Network                  │
│                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ CaraBot  │  │PostgreSQL│  │  Redis   │  │
│  │  :8000   │  │  :5432   │  │  :6379   │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  │
│       │              │              │        │
│  ┌────┴──────────────┴──────────────┴─────┐  │
│  │              Milvus                     │  │
│  │  :19530 (gRPC)  :9091 (metrics)        │  │
│  │    ├── etcd (元数据) :2379              │  │
│  │    └── MinIO (对象存储) :9000           │  │
│  └─────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

### 2.2 docker-compose.yml 核心配置

```yaml
services:
  carabot:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY}
      - VECTOR_STORE_TYPE=milvus
      - DB_URL=postgresql+asyncpg://carabot:carabot@postgres:5432/carabot
      - REDIS_URL=redis://redis:6379/0
      - MILVUS_HOST=milvus-standalone
      - MILVUS_PORT=19530
      - LLM_BASE_URL=${LLM_BASE_URL}
      - LLM_API_KEY=${LLM_API_KEY}
      - LLM_MODEL=${LLM_MODEL}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: carabot
      POSTGRES_PASSWORD: carabot
      POSTGRES_DB: carabot
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U carabot"]

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]

  milvus-standalone:
    image: milvusdb/milvus:v2.4.0
    ports:
      - "19530:19530"
      - "9091:9091"
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
    depends_on:
      - etcd
      - minio

  etcd:
    image: quay.io/coreos/etcd:v3.5.5
    # ...

  minio:
    image: minio/minio:latest
    # ...

volumes:
  postgres_data:
  redis_data:
  milvus_data:
```

### 2.3 depends_on + healthcheck 保证启动顺序

```yaml
depends_on:
  postgres:
    condition: service_healthy  # 等 PostgreSQL 真正可用（不只是容器启动了）
```

---

## 3. 部署命令

```bash
# 1. 构建并启动所有服务
docker-compose up -d

# 2. 查看日志
docker-compose logs -f carabot

# 3. 验证部署
curl http://localhost:8000/health

# 4. 查看所有服务状态
docker-compose ps

# 5. 停止所有服务
docker-compose down

# 6. 停止并删除数据卷
docker-compose down -v
```

---

## 4. 课后作业

1. **构建镜像**：执行 `docker build -t carabot .`，记录构建时间和镜像大小
2. **分析镜像层**：用 `docker history carabot` 分析每一层的大小
3. **Docker Compose 部署**：用 docker-compose 启动全套服务，验证各组件连通性
4. **体积优化**：尝试将镜像基础从 `slim` 换成 `alpine`，看能否进一步减小体积
5. **安全扫描**：用 `docker scout` 或 `trivy` 扫描镜像的安全漏洞

---

## 📚 扩展阅读
- Docker 多阶段构建：https://docs.docker.com/build/building/multi-stage/
- Docker Compose 文档：https://docs.docker.com/compose/
- Dockerfile 最佳实践：https://docs.docker.com/develop/develop-images/dockerfile_best-practices/

---

**下节课预告**：第 29 课 - 生产环境配置
