# CaraBot Agent 智能知识库系统课程
## 第 14 课：向量嵌入技术

### 🎯 本节课目标
- 理解文本向量嵌入的数学直觉：把语义变成数字
- 掌握 BGE-m3 多语言嵌入模型的特性和使用
- 深入分析 CaraBot 的 EmbeddingService 实现
- 学会对比不同嵌入模型的性能
- 学习时长：3 天
- 实践任务：使用不同模型生成向量

---

## 1. 什么是文本向量？

### 1.1 直觉理解

把一段文字变成一个**固定长度的数字数组**，让语义相近的文字在向量空间中也相近：

```
"今天天气很好"  →  [0.12, -0.34, 0.56, ..., 0.78]  (1024 个数字)
"今天天气不错"  →  [0.13, -0.33, 0.55, ..., 0.77]  ← 向量很接近！
"量子力学原理"  →  [-0.89, 0.42, -0.15, ..., 0.21] ← 向量差很远！
```

### 1.2 向量空间的几何直觉

```
              "量子力学" ●
                         
                         
    "今天天气好" ●  ← 距离很近 →  ● "今日天气佳"
                         
                         
              "人工智能" ●
                         
    ─────────────────────────────
    维度 1：天气相关 ← → 科技相关
```

**余弦相似度**：两个向量之间的夹角越小 → 语义越接近。

---

## 2. BGE-m3 模型详解

### 2.1 为什么选 BGE-m3？

| 特性 | BGE-m3 | OpenAI text-embedding-3 | Cohere Embed v3 |
|------|--------|------------------------|-----------------|
| 开源 | ✅ 完全开源 | ❌ 闭源 API | ❌ 闭源 API |
| 向量维度 | 1024 | 256-3072 | 1024 |
| 多语言 | 100+ 语言 | 主要英文 | 主要英文 |
| 最大长度 | 8192 tokens | 8191 tokens | 512 tokens |
| 本地部署 | ✅ 可以 | ❌ 必须联网 | ❌ 必须联网 |
| 费用 | 免费（GPU 成本） | $0.02/1M tokens | 按查询收费 |

**CaraBot 的场景**（企业知识库、多语言、数据敏感）→ BGE-m3 是最佳选择。

### 2.2 BGE-m3 的三个"M"

- **Multi-Lingual**（多语言）：支持中英日韩法德等 100+ 种语言
- **Multi-Functionality**（多功能）：同时支持 Dense 检索和 Sparse（BM25 风格）检索
- **Multi-Granularity**（多粒度）：支持短句到长文档（8192 tokens）

---

## 3. EmbeddingService 完整实现

### 3.1 模型加载

```python
class EmbeddingService:
    def __init__(self, settings: Settings):
        self._model_name = settings.bge_model_name      # "BAAI/bge-m3"
        self._model_path = settings.bge_model_path       # "./data/models/bge-m3"
        self._device = settings.bge_device               # "cpu" 或 "cuda"
        self._batch_size = settings.bge_batch_size       # 32
        self._model = None

    def _load_model(self):
        from sentence_transformers import SentenceTransformer

        model_path = self._resolve_model_path()
        self._model = SentenceTransformer(
            model_path,
            device=self._device,
            trust_remote_code=True,
        )

        # 预热：跑一次空推理，触发 CUDA 编译
        _ = self._model.encode(["warmup"], batch_size=1, normalize_embeddings=True)
        dim = self._model.get_sentence_embedding_dimension()
        logger.info("Embedding model loaded. Dimension: %s", dim)
```

### 3.2 模型路径解析策略

```python
def _resolve_model_path(self) -> str:
    local_dir = Path(self._model_path)
    if local_dir.exists() and any(local_dir.iterdir()):
        # 策略 1：本地有缓存 → 直接用
        logger.info("Using cached model at %s", local_dir)
        return str(local_dir)

    # 策略 2：从 HuggingFace 下载（自动缓存到 ~/.cache/huggingface/）
    logger.info("Loading model '%s' (auto-cached by HuggingFace)", self._model_name)
    return self._model_name
```

### 3.3 批量文档嵌入

```python
async def embed_documents(self, texts: list[str]) -> list[list[float]]:
    if not self._model:
        raise EmbeddingException("Model not loaded. Call startup() first.")

    if not texts:
        return []

    embeddings = await asyncio.to_thread(
        self._model.encode,
        texts,
        batch_size=self._batch_size,        # 一次处理 32 条
        normalize_embeddings=True,          # 归一化（便于计算余弦相似度）
        show_progress_bar=False,
    )
    return embeddings.tolist()              # numpy → Python list
```

### 3.4 单条查询嵌入

```python
async def embed_query(self, text: str) -> list[float]:
    embedding = await asyncio.to_thread(
        self._model.encode,
        [text],
        batch_size=1,
        normalize_embeddings=True,
    )
    return embedding[0].tolist()
```

### 3.5 normalize_embeddings 的作用

归一化后所有向量长度 = 1。此时：
- 内积（dot product）= 余弦相似度
- 计算更快（不需要除以向量模长）

---

## 4. 实践：对比不同嵌入模型

### 4.1 测试脚本

```python
import time
from sentence_transformers import SentenceTransformer

models = {
    "bge-m3": "BAAI/bge-m3",
    "bge-large-zh": "BAAI/bge-large-zh-v1.5",
    "all-MiniLM-L6": "sentence-transformers/all-MiniLM-L6-v2",
}

test_texts = [
    "什么是检索增强生成？",
    "RAG 是如何工作的？",
    "今天天气真好",
    "向量数据库有哪些类型？",
]

for name, model_id in models.items():
    model = SentenceTransformer(model_id)
    start = time.time()
    embeddings = model.encode(test_texts, normalize_embeddings=True)
    elapsed = time.time() - start

    dim = embeddings.shape[1]
    # 计算语义相似度
    from sklearn.metrics.pairwise import cosine_similarity
    sim = cosine_similarity([embeddings[0]], [embeddings[1]])[0][0]

    print(f"{name}: dim={dim}, time={elapsed:.2f}s, sim(q1,q2)={sim:.4f}")
```

### 4.2 预期结果

```
bge-m3:         dim=1024, time=2.34s, sim(q1,q2)=0.9234  ← 中英文都好
bge-large-zh:   dim=1024, time=1.89s, sim(q1,q2)=0.9456  ← 中文更好
all-MiniLM-L6:  dim=384,  time=0.56s, sim(q1,q2)=0.7123  ← 中文一般
```

---

## 5. 嵌入维度与存储成本

| 维度 | 每条向量大小 | 100 万条存储 | 检索速度 |
|------|------------|------------|---------|
| 384 | 1.5 KB | 1.5 GB | 快 |
| 768 | 3.0 KB | 3.0 GB | 中 |
| **1024** (BGE-m3) | 4.0 KB | 4.0 GB | 中 |
| 1536 | 6.0 KB | 6.0 GB | 慢 |
| 3072 | 12.0 KB | 12.0 GB | 很慢 |

---

## 6. 课后作业

1. **运行 BGE-m3**：安装 `sentence-transformers`，加载 BGE-m3 模型，对 10 条中文和 10 条英文文本生成嵌入
2. **语义相似度实验**：用余弦相似度比较以下句对，验证预期：
   - "苹果很好吃" vs "苹果真好吃" → 应 > 0.9
   - "苹果很好吃" vs "苹果公司发布了新手机" → 应 < 0.5
   - "RAG is great" vs "检索增强生成很棒" → 跨语言测试
3. **维度对比**：对比 BGE-m3 (1024d) 和 all-MiniLM-L6 (384d) 在同一批测试数据上的检索精度
4. **硬件测试**：对比 `device=cpu` 和 `device=cuda` 下的嵌入速度差异
5. **温度测试**：对 BGE-m3 做预热 vs 不做预热的首次推理耗时对比

---

## 📚 扩展阅读
- BGE-m3 论文：https://arxiv.org/abs/2402.03216
- Sentence-Transformers 文档：https://www.sbert.net/
- MTEB 排行榜：https://huggingface.co/spaces/mteb/leaderboard

---

**下节课预告**：第 15 课 - 向量数据库基础（向量空间模型、相似性计算、与传统数据库的差异）
