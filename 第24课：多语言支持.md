# CaraBot Agent 智能知识库系统课程
## 第 24 课：多语言支持

### 🎯 本节课目标
- 理解 BGE-m3 的多语言嵌入原理
- 掌握跨语言语义搜索的工作方式
- 学习语言检测与多语言 RAG 评估
- 学习时长：2 天
- 实践任务：测试跨语言搜索功能

---

## 1. BGE-m3 多语言能力

### 1.1 支持的语言

BGE-m3 在 100+ 种语言上训练，包括：
- 中文、英文、日语、韩语、法语、德语、西班牙语
- 阿拉伯语、俄语、葡萄牙语、印地语
- **斯瓦希里语**（传音非洲市场的重要语言）

### 1.2 跨语言检索原理

BGE-m3 将不同语言的语义相近文本映射到向量空间的相近位置：

```
"今天天气很好"  (中文)
"What a nice day" (英文)  
"Hali ya hewa ni nzuri" (斯瓦希里语)
    ↓ 嵌入
  [0.12, -0.34, 0.56, ..., 0.78]  ← 三个向量距离很近！
```

### 1.3 跨语言搜索实战

```python
# 用英文查询，搜索中文文档
results = await search_service.search(
    query="annual leave policy",  # 英文查询
    top_k=5,
)
# 返回："公司年假政策：员工每年享有 15 天带薪年假..."  ← 中文文档！
```

**不需要翻译**！BGE-m3 的向量空间自动处理了跨语言语义对齐。

---

## 2. 多语言评估

### 2.1 按语言分组评估

```python
test_queries = [
    EvalQuery(query="年假政策", relevant_doc_ids=["doc_001"], language="zh"),
    EvalQuery(query="annual leave", relevant_doc_ids=["doc_001"], language="en"),
    EvalQuery(query="likizo ya mwaka", relevant_doc_ids=["doc_001"], language="sw"),
]

result = await evaluator.evaluate(test_queries)

# 输出：
# per_language: {
#   "zh": {"recall@5": 0.95},
#   "en": {"recall@5": 0.92},
#   "sw": {"recall@5": 0.68},  ← 斯瓦希里语效果较差
# }
```

### 2.2 多语言优化策略

| 策略 | 说明 | 成本 |
|------|------|------|
| 查询翻译 | 先翻译成主要语言再搜索 | 低（调用翻译 API） |
| 文档多语言索引 | 同一文档用多种语言分别索引 | 中（存储翻倍） |
| 模型微调 | 在目标语言上微调 BGE-m3 | 高（需要标注数据） |

---

## 3. 语言检测

```python
# 可选：用 langdetect 库检测语言
from langdetect import detect

query = "今天天气如何"
lang = detect(query)  # "zh-cn"

# 根据语言调整搜索策略
if lang in low_resource_languages:
    # 先用翻译模型翻译成英文
    query_en = translate(query, target="en")
    results = await search(query_en)
else:
    results = await search(query)
```

---

## 4. 课后作业

1. **跨语言测试**：上传中文文档，用英文查询搜索，验证能否召回
2. **多语言对比**：用同一语义的中、英、日、斯瓦希里语查询，对比搜索结果的 Recall@5
3. **语言检测**：集成 `langdetect` 库，自动检测查询语言并记录到搜索日志
4. **翻译管道**：实现一个"查询翻译 → 英文搜索 → 结果翻译回"的跨语言搜索管道，对比直搜效果

---

## 📚 扩展阅读
- BGE-m3 多语言能力：https://huggingface.co/BAAI/bge-m3
- 跨语言检索综述：https://arxiv.org/abs/2101.07658

---

**下节课预告**：第 25 课 - 缓存与性能优化
