# CaraBot Agent 智能知识库系统课程
## 第 8 课：中间件机制

### 🎯 本节课目标
- 理解中间件的概念：请求管道中的横切关注点处理器
- 深入分析 CaraBot 三层中间件：日志 → 认证 → 错误处理
- 掌握 FastAPI/Starlette 中间件的两种实现方式（BaseHTTPMiddleware vs 依赖注入）
- 学习 trace_id 的 contextvars 实现原理
- 学习时长：1 天
- 实践任务：自定义中间件

---

## 1. 中间件是什么？

### 1.1 洋葱模型

HTTP 请求像穿过一层层洋葱皮，每一层中间件都可以在请求到达业务逻辑**之前**和**之后**做处理：

```
        请求 ↓                         响应 ↑
    ┌─────────────────────────────────────────┐
    │  RequestLoggingMiddleware（日志）        │
    │  ┌───────────────────────────────────┐  │
    │  │  AuthGuard（认证）                 │  │
    │  │  ┌─────────────────────────────┐  │  │
    │  │  │  ErrorHandler（错误处理）    │  │  │
    │  │  │  ┌───────────────────────┐  │  │  │
    │  │  │  │  业务逻辑（Routes）    │  │  │  │
    │  │  │  └───────────────────────┘  │  │  │
    │  │  └─────────────────────────────┘  │  │
    │  └───────────────────────────────────┘  │
    └─────────────────────────────────────────┘
```

**请求方向**（外 → 内）：日志 → 认证 → 错误处理 → 业务逻辑
**响应方向**（内 → 外）：业务逻辑 → 错误处理 → 认证 → 日志

### 1.2 什么是横切关注点？

有些逻辑跟具体业务无关，但每个接口都需要：

| 关注点 | 如果没有中间件 | 有了中间件 |
|--------|-------------|-----------|
| 日志 | 每个接口手动 `print(method, path)` | 中间件自动记录所有请求 |
| 认证 | 每个接口手动检查 API Key | 中间件统一拦截 |
| 错误处理 | 每个接口 try-except + 返回 JSON | 全局错误处理器统一格式化 |
| CORS | 每个接口手动加 header | 一个中间件搞定 |
| 限流 | 每个接口手动计数 | 中间件统一管理 |

---

## 2. CaraBot 的中间件注册顺序

在 `main.py` 的 `create_app()` 中：

```python
app = FastAPI(title="CaraBot", ...)

# 注册顺序 = 执行顺序（外→内）
app.add_middleware(RequestLoggingMiddleware)  # ① 最外层：记录所有请求
register_error_handlers(app)                  # ② 错误处理器
# AuthGuard 不在这里注册，而是作为 Depends 注入到各路由
```

**为什么 AuthGuard 不用 `add_middleware`？**

因为健康检查接口（`/health`）不需要认证。如果用中间件，就需要在中间件里写路径白名单。用 `Depends` 可以让每个路由自己决定是否需要认证。

---

## 3. 第一层：RequestLoggingMiddleware（日志中间件）

### 3.1 完整代码

```python
from starlette.middleware.base import BaseHTTPMiddleware

class RequestLoggingMiddleware(BaseHTTPMiddleware):

    async def dispatch(self, request: Request, call_next: RequestResponseEndpoint) -> Response:
        # === 请求进来时 ===
        trace_id = set_trace_id()              # ① 生成 trace_id
        request.state.trace_id = trace_id       # ② 绑到 request.state 上
        start_time = time.perf_counter()        # ③ 开始计时

        # === 调用下一层（进入业务逻辑） ===
        response = await call_next(request)

        # === 响应出去时 ===
        duration_ms = (time.perf_counter() - start_time) * 1000

        response.headers["X-Trace-ID"] = trace_id   # ④ 把 trace_id 返回给客户端

        logger.info(                                  # ⑤ 记录一条汇总日志
            "%s %s → %d (%.1fms)",
            request.method, request.url.path, response.status_code, duration_ms,
        )

        return response
```

### 3.2 执行流程分解

```
1. 请求到达
2. set_trace_id() → 生成 UUID "a1b2c3d4-e5f6-..."
3. request.state.trace_id = "a1b2c3d4..."
4. await call_next(request) → 进入认证 → 进入路由 → 执行业务逻辑
5. 响应回来，计算耗时 = 45.2ms
6. response.headers["X-Trace-ID"] = "a1b2c3d4..."
7. logger.info("POST /api/v1/search → 200 (45.2ms)")
8. 返回 response
```

### 3.3 trace_id 的魔法：contextvars

问题：`set_trace_id()` 是怎么让后续的代码都能拿到同一个 trace_id 的？

答案：Python 的 `contextvars`——一个协程安全的"线程局部变量"：

```python
from contextvars import ContextVar

_trace_id_var: ContextVar[str] = ContextVar("trace_id", default="")

def get_trace_id() -> str:
    return _trace_id_var.get()    # 获取当前协程的 trace_id

def set_trace_id(trace_id=None) -> str:
    tid = trace_id or str(uuid.uuid4())
    _trace_id_var.set(tid)        # 设置当前协程的 trace_id
    return tid
```

**关键**：每个请求都在独立的协程中处理。`ContextVar` 保证协程 A 拿不到协程 B 的 trace_id，即使它们同时执行。

### 3.4 trace_id 的使用场景

有了 trace_id，排查问题非常方便：

```bash
# 客户端报错时提供了 X-Trace-ID
# 在日志中搜索这个 trace_id：
grep "a1b2c3d4" ./logs/carabot.log

# 得到完整请求链路：
# {"timestamp":"...", "level":"INFO", "message":"POST /api/v1/upload → 200 (523ms)", "trace_id":"a1b2c3d4"}
# {"timestamp":"...", "level":"INFO", "message":"Processed 'doc.pdf': 1 docs → 23 chunks", "trace_id":"a1b2c3d4"}
# {"timestamp":"...", "level":"INFO", "message":"Loaded embedding model", "trace_id":"a1b2c3d4"}
```

---

## 4. 第二层：AuthGuard（认证）

### 4.1 为什么不用中间件？

CaraBot 的 `AuthGuard` 是一个**可调用类**，通过 FastAPI 的 `Depends()` 注入：

```python
class AuthGuard:
    def __init__(self, settings: Settings):
        self._api_key = settings.api_key

    async def __call__(self, x_api_key: str | None = Header(alias="X-API-Key") = None) -> str:
        if not x_api_key:
            raise MissingApiKeyException()      # 400
        if x_api_key != self._api_key:
            raise InvalidApiKeyException()      # 401
        return x_api_key
```

在路由中使用：
```python
@router.post("/chat")
async def chat(
    body: ChatRequest,
    _api_key: str = Depends(auth_guard),  # ← 只有需要认证的路由加这一行
):
    ...
```

**好处**：健康检查路由不加 `Depends(auth_guard)`，自然跳过认证。

### 4.2 用中间件 vs 用 Depends 对比

| 维度 | 中间件方式 | Depends 方式（CaraBot） |
|------|----------|----------------------|
| 覆盖范围 | 所有请求 | 按路由选择 |
| 白名单 | 需要手动写路径判断 | 不加 Depends 即可 |
| 灵活性 | 低 | 高（不同路由可以有不同的认证逻辑） |
| 性能 | 略好（注册一次） | 略差（每个路由解析依赖） |

### 4.3 认证失败的响应

```json
// 401 Unauthorized
{
  "error": {
    "code": "INVALID_API_KEY",
    "message": "Invalid API key.",
    "trace_id": "a1b2c3d4-e5f6-...",
    "detail": null
  }
}
```

---

## 5. 第三层：全局错误处理

### 5.1 三层异常体系

```
Exception（Python 内置）
  └── CaraBotException（CaraBot 所有异常的基类）
        ├── AuthException（认证相关）
        │     ├── MissingApiKeyException（400）
        │     └── InvalidApiKeyException（401）
        ├── DocumentException（文档处理相关）
        │     ├── UnsupportedFileTypeException（415）
        │     └── FileTooLargeException（413）
        ├── AgentException（Agent 相关）
        │     ├── LLMException（502）
        │     ├── ToolExecutionException（500）
        │     └── ConversationNotFoundException（404）
        ├── EmbeddingException（500）
        ├── VectorStoreException（500）
        ├── ConfigurationException（500）
        └── HealthCheckException（503）
```

每个异常自带 `code`（错误码）、`status_code`（HTTP 状态码）、`message`（默认错误信息）。

### 5.2 统一错误处理器

```python
def register_error_handlers(app: FastAPI):
    # ① 处理所有 CaraBot 异常
    @app.exception_handler(CaraBotException)
    async def carabot_exception_handler(request, exc):
        return JSONResponse(
            status_code=exc.status_code,
            content={
                "error": {
                    "code": exc.code,          # 如 "UNSUPPORTED_FILE_TYPE"
                    "message": exc.message,     # 如 "Unsupported file type."
                    "trace_id": get_trace_id(),
                    "detail": exc.detail,
                }
            },
        )

    # ② 兜底：处理意料之外的异常
    @app.exception_handler(Exception)
    async def unhandled_exception_handler(request, exc):
        logger.exception("Unhandled exception: %s", exc)
        return JSONResponse(
            status_code=500,
            content={
                "error": {
                    "code": "INTERNAL_ERROR",
                    "message": "An unexpected internal error occurred.",
                    "trace_id": get_trace_id(),
                }
            },
        )
```

### 5.3 错误响应格式一致性

所有错误响应都是相同的 JSON 结构：
```json
{
  "error": {
    "code": "...",       // 机器可读的错误码
    "message": "...",    // 人类可读的错误描述
    "trace_id": "...",   // 用于日志追踪
    "detail": null       // 可选的额外信息
  }
}
```

前端只需要判断 `error.code` 即可做不同的错误处理。

---

## 6. 实践：自定义中间件

### 6.1 需求：添加请求频率限制中间件

```python
import time
from collections import defaultdict
from starlette.middleware.base import BaseHTTPMiddleware

class RateLimitMiddleware(BaseHTTPMiddleware):
    def __init__(self, app, max_requests: int = 100, window_seconds: int = 60):
        super().__init__(app)
        self._max_requests = max_requests
        self._window = window_seconds
        self._clients: dict[str, list[float]] = defaultdict(list)

    async def dispatch(self, request, call_next):
        # 获取客户端标识（用 API Key 或 IP）
        client_id = request.headers.get("X-API-Key") or request.client.host

        # 清理过期记录
        now = time.time()
        self._clients[client_id] = [
            t for t in self._clients[client_id]
            if now - t < self._window
        ]

        # 检查是否超限
        if len(self._clients[client_id]) >= self._max_requests:
            return JSONResponse(
                status_code=429,
                content={
                    "error": {
                        "code": "RATE_LIMITED",
                        "message": f"Too many requests. Max {self._max_requests} per {self._window}s.",
                    }
                },
            )

        # 记录本次请求
        self._clients[client_id].append(now)

        # 继续处理
        return await call_next(request)

# 注册
app.add_middleware(RateLimitMiddleware, max_requests=100, window_seconds=60)
```

### 6.2 需求：添加响应时间 Header

```python
class TimingMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request, call_next):
        start = time.perf_counter()
        response = await call_next(request)
        elapsed = (time.perf_counter() - start) * 1000
        response.headers["X-Response-Time-Ms"] = f"{elapsed:.1f}"
        return response
```

---

## 7. 中间件 vs 依赖注入 vs 装饰器：什么时候用哪个？

| 场景 | 推荐 | 原因 |
|------|------|------|
| 所有请求都要处理（日志、CORS） | 中间件 | 一次注册，全局生效 |
| 部分接口需要（认证） | Depends | 按路由选择性使用 |
| 单个接口的特殊逻辑 | 装饰器 | 粒度最细 |
| 异常处理 | `@app.exception_handler` | FastAPI 原生支持 |

---

## 8. 课后作业

1. **跟踪中间件执行**：在 `RequestLoggingMiddleware.dispatch` 中加断点/打印，观察一次请求的完整执行顺序
2. **分析 trace_id 流转**：画图说明 trace_id 从生成到返回给客户端的完整生命周期
3. **实现自定义中间件**：编写一个 `UserAgentMiddleware`，记录每个请求的 User-Agent 到日志
4. **设计新的异常类**：为 "并发上传限制" 设计异常类 `ConcurrentUploadLimitException`，继承 `CaraBotException`，设置合适的 code 和 status_code
5. **对比 Java**：用 Spring Boot 的 `Filter` 和 `@ControllerAdvice` 实现相同的认证和错误处理，对比代码量

---

## 📚 扩展阅读
- Starlette Middleware：https://www.starlette.io/middleware/
- Python contextvars：https://docs.python.org/3/library/contextvars.html
- FastAPI 异常处理：https://fastapi.tiangolo.com/tutorial/handling-errors/

---

**下节课预告**：第 9 课 - 异步编程在 Python 中的应用（async/await、asyncio.to_thread、数据库异步操作）
