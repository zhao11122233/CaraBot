# CaraBot Agent 智能知识库系统课程
## 第 5 课：配置系统与环境变量

### 🎯 本节课目标
- 深入理解 Pydantic Settings 的配置管理机制
- 掌握环境变量加载与 .env 文件的映射规则
- 学习配置验证、交叉字段验证和错误处理
- 对比 Python 配置系统与 Spring Boot 的差异
- 学习时长：1 天
- 实践任务：自定义配置项并验证

---

## 1. 配置系统概述

### 1.1 为什么需要配置系统？

任何企业级应用都需要管理大量配置项：数据库连接、API 密钥、模型参数、日志级别等。把这些值硬编码在源码中会导致：

- **安全风险**：密钥泄露到代码仓库
- **部署困难**：不同环境（开发/测试/生产）需要修改代码
- **维护噩梦**：配置散落在各个文件中，难以追踪

CaraBot 使用 **Pydantic Settings** 构建了一个集中、类型安全、自动验证的配置系统。

### 1.2 Pydantic Settings 是什么？

Pydantic Settings 是 Pydantic 生态的扩展库，专为配置管理设计：

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    api_key: str            # 自动从环境变量 API_KEY 读取
    port: int = 8000        # 有默认值则可选
    log_level: str = "INFO" # 自带默认值
```

**核心能力**：
- 自动从环境变量读取（`API_KEY` → `api_key`）
- 支持 `.env` 文件加载
- 完整的类型验证和转换
- 自定义验证器
- 与 Pydantic 模型相同的 Field 约束

### 1.3 配置加载流程图

```
启动应用
  │
  ▼
load_settings()
  │
  ├── 1. 读取 .env 文件
  │     └── 解析 KEY=VALUE 格式
  │
  ├── 2. 读取系统环境变量
  │     └── 环境变量优先级高于 .env 文件
  │
  ├── 3. Pydantic 类型转换
  │     └── "8000" (str) → 8000 (int)
  │
  ├── 4. Field 约束验证
  │     └── ge=1, le=65535, min_length=8...
  │
  ├── 5. 自定义验证器执行
  │     └── @field_validator 装饰的方法
  │
  ├── 6. 创建必要目录
  │     └── chroma_persist_dir, bge_model_path...
  │
  └── 7. 返回 Settings 实例
```

---

## 2. Settings 类详解

### 2.1 类定义与元配置

```python
from pydantic import Field
from pydantic_settings import BaseSettings, SettingsConfigDict

class Settings(BaseSettings):
    """Central configuration for CaraBot."""

    model_config = SettingsConfigDict(
        env_file=".env",              # 从 .env 文件加载
        env_file_encoding="utf-8",    # 文件编码
        extra="forbid",               # 禁止未定义的字段，防止拼写错误
    )
```

`extra="forbid"` 是一个重要的安全措施。如果 `.env` 中有拼写错误的字段名（比如 `API_KYE=xxx`），应用会**立即报错**而不是静默忽略，避免因配置未生效导致的诡异 bug。

### 2.2 Field 的使用

`Field()` 是 Pydantic 配置字段的核心方法。每个配置项通过它定义默认值、约束和描述：

```python
# 必填字段（无默认值）
api_key: str = Field(..., min_length=8, description="API Key for request authentication")

# 可选字段（有默认值）
port: int = Field(default=8000, ge=1, le=65535, description="Server port")

# Literal 类型约束
vector_store_type: Literal["milvus", "chroma"] = Field(
    default="milvus", description="Vector store backend: milvus or chroma"
)
```

**Field 常用参数**：

| 参数 | 说明 | 示例 |
|------|------|------|
| `default` | 默认值，`...` 表示必填 | `default=8000` |
| `ge` / `le` | 数值的最小/最大值 | `ge=1, le=65535` |
| `min_length` / `max_length` | 字符串长度限制 | `min_length=8` |
| `description` | 字段说明（生成文档用） | `description="Server port"` |
| `gt` / `lt` | 严格大于/小于 | `gt=0` |

### 2.3 配置组一览

CaraBot 的 Settings 按功能分为 10 个配置组。我们逐一分析。

#### 2.3.1 API 认证

```python
api_key: str = Field(..., min_length=8, description="API Key for request authentication")
```

唯一必填字段。`...` 表示没有默认值，必须在 `.env` 或环境变量中提供。

对应的 `.env` 配置：
```bash
API_KEY=carabot-secret-key-change-me
```

#### 2.3.2 向量存储

```python
vector_store_type: Literal["milvus", "chroma"] = Field(default="milvus")

# Milvus
milvus_host: str = Field(default="localhost")
milvus_port: int = Field(default=19530, ge=1, le=65535)
milvus_user: str = Field(default="root")
milvus_password: str = Field(default="Milvus")
milvus_collection_name: str = Field(default="carabot_knowledge")
milvus_dimension: int = Field(default=1024)

# Chroma
chroma_persist_dir: str = Field(default="./data/chroma")
chroma_collection_name: str = Field(default="carabot_knowledge")
```

设计要点：Milvus（生产）和 Chroma（开发）共享 `vector_store_type` 开关。当类型为 `milvus` 时，Milvus 相关字段通过验证器确保非空。

#### 2.3.3 嵌入模型

```python
bge_model_name: str = Field(default="BAAI/bge-m3")
bge_model_path: str = Field(default="./data/models/bge-m3")
bge_device: str = Field(default="cpu")
bge_batch_size: int = Field(default=32, ge=1, le=256)
```

`bge_model_path` 指定本地模型缓存路径。首次运行时如果本地没有模型，会自动从 HuggingFace 下载。

#### 2.3.4 数据库

```python
db_url: str = Field(
    default="postgresql+asyncpg://carabot:carabot@localhost:5432/carabot"
)
db_url_sync: str = Field(
    default="postgresql+psycopg2://carabot:carabot@localhost:5432/carabot"
)
```

两个 URL 的区别：`db_url` 使用 `asyncpg` 异步驱动（运行时），`db_url_sync` 使用 `psycopg2` 同步驱动（Alembic 迁移用）。

**连接字符串格式**：
```
postgresql+asyncpg://用户名:密码@主机:端口/数据库名
```

#### 2.3.5 Redis

```python
redis_url: str = Field(default="redis://localhost:6379/0")
redis_index_queue: str = Field(default="carabot:index:queue")
```

Redis 在 CaraBot 中是可选的（用于任务队列和缓存），启动失败不会阻塞服务。

#### 2.3.6 文档处理

```python
chunk_size: int = Field(default=500, ge=100, le=10000)
chunk_overlap: int = Field(default=50, ge=0, le=1000)
max_file_size: int = Field(default=52_428_800, ge=1)  # 50MB
```

这里有一个重要的**交叉字段约束**：`chunk_overlap` 必须小于 `chunk_size`。如果 overlap ≥ chunk_size，文本块将无限循环重叠，导致嵌入失败。

#### 2.3.7 检索

```python
top_k: int = Field(default=5, ge=1, le=100)
similarity_threshold: float = Field(default=0.7, ge=0.0, le=1.0)
```

`top_k` 控制返回结果数量，`similarity_threshold` 过滤低相似度的结果（0.0-1.0）。

#### 2.3.8 日志

```python
log_level: Literal["DEBUG", "INFO", "WARNING", "ERROR"] = Field(default="INFO")
log_file: str = Field(default="./logs/carabot.log")
log_format: Literal["json", "text"] = Field(default="json")
```

生产环境建议 `json` 格式，便于 ELK 等日志系统解析；开发环境可切换 `text` 格式便于肉眼阅读。

#### 2.3.9 LLM 配置（LangGraph Agent）

```python
llm_base_url: str = Field(default="http://localhost:8000/v1")
llm_api_key: str = Field(default="not-needed")
llm_model: str = Field(default="gpt-4o-mini")
llm_temperature: float = Field(default=0.1, ge=0.0, le=2.0)
llm_max_tokens: int = Field(default=2048, ge=1, le=16384)
```

`llm_base_url` 支持所有 OpenAI 兼容接口，可对接：
- **OpenAI**：`https://api.openai.com/v1`
- **DeepSeek**：`https://api.deepseek.com/v1`
- **本地模型**：`http://localhost:8000/v1`（vLLM/Ollama）

`temperature=0.1` 让 Agent 的回复更精确、可预测，适合知识库问答场景。

#### 2.3.10 Agent 配置

```python
agent_max_iterations: int = Field(default=10, ge=1, le=50)
agent_system_prompt: str = Field(default="You are CaraBot, ...")
agent_checkpoint_db_url: str = Field(default="")
```

- `agent_max_iterations`：防止 Agent 在 LLM→工具→LLM 循环中无限递归
- `agent_system_prompt`：定义 Agent 的人设和行为规范
- `agent_checkpoint_db_url`：对话记录持久化地址，空值时自动从 `db_url` 推导

#### 2.3.11 服务器

```python
host: str = Field(default="0.0.0.0")
port: int = Field(default=8000, ge=1, le=65535)
workers: int = Field(default=4, ge=1, le=16)
```

---

## 3. 自定义验证器

Pydantic 的 `@field_validator` 装饰器可以在字段赋值后执行自定义校验逻辑。CaraBot 使用了 5 个验证器。

### 3.1 单字段验证

```python
@field_validator("api_key")
@classmethod
def api_key_must_not_be_default(cls, v: str) -> str:
    """禁止使用不安全的默认 API Key。"""
    if v in ("change-me", "changeme", "your-api-key-here"):
        raise ValueError(
            "API_KEY is set to an insecure default value. "
            "Please set a strong API_KEY in your .env file."
        )
    return v
```

**执行时机**：`api_key` 字段被赋值后立即执行。

**返回值**：验证器必须返回处理后的值（可以修改或保持原样）。

### 3.2 条件必填验证

```python
@field_validator("db_url")
@classmethod
def db_url_must_be_valid_async_driver(cls, v: str) -> str:
    """确保数据库 URL 使用异步驱动。"""
    valid_drivers = ("asyncpg", "aiosqlite", "sqlite+aiosqlite")
    if not any(d in v for d in valid_drivers):
        raise ValueError(
            f"DB_URL must use an async driver, got: {v}"
        )
    return v
```

如果用户误用了同步驱动（例如 `postgresql+psycopg2://...`），启动时会立即得到清晰的错误信息。

### 3.3 交叉字段验证

```python
@field_validator("chunk_overlap")
@classmethod
def overlap_less_than_chunk_size(cls, v: int, info) -> int:
    """确保 chunk_overlap 小于 chunk_size。"""
    chunk_size = info.data.get("chunk_size", 0)
    if v >= chunk_size:
        raise ValueError(
            f"CHUNK_OVERLAP ({v}) must be less than CHUNK_SIZE ({chunk_size})"
        )
    return v
```

**关键参数 `info`**：`info.data` 是一个字典，包含**当前已验证的**其他字段值。注意：只有已经被验证过的字段才会出现在 `info.data` 中，依赖的字段必须在验证顺序上靠前。

### 3.4 条件验证

```python
@field_validator("milvus_user", "milvus_host")
@classmethod
def milvus_config_required_when_milvus_selected(cls, v: str, info) -> str:
    """当选择 Milvus 时，Milvus 配置不能为空。"""
    store_type = info.data.get("vector_store_type", "chroma")
    if store_type == "milvus" and not v:
        raise ValueError(
            f"Field '{info.field_name}' is required when VECTOR_STORE_TYPE=milvus"
        )
    return v
```

这个验证器同时应用于 `milvus_user` 和 `milvus_host`，只有当 `vector_store_type=milvus` 时才要求非空。当使用 Chroma 时，Milvus 字段可以为空。

### 3.5 多字段验证器

```python
@field_validator("chroma_persist_dir", "log_file", "bge_model_path")
@classmethod
def ensure_parent_dirs_exist(cls, v: str) -> str:
    """确保路径的父目录存在。"""
    path = Path(v)
    if not path.is_absolute() and not v.startswith("./"):
        pass  # 相对路径跳过
    return v
```

一个验证器可以同时注册到多个字段，通过传入字段名列表实现。

---

## 4. load_settings 函数

### 4.1 完整流程

```python
def load_settings() -> Settings:
    """加载并验证所有配置。验证失败则打印清晰错误并退出。"""
    try:
        settings = Settings()  # 触发 Pydantic 完整的加载+验证流程
    except ValidationError as exc:
        # 格式化错误信息，清晰展示每个配置问题
        print("=" * 60)
        print("CONFIGURATION ERROR: Missing or invalid configuration values")
        print("=" * 60)
        for error in exc.errors():
            loc = " -> ".join(str(x) for x in error["loc"])
            msg = error["msg"]
            print(f"  [{loc}] {msg}")
        print("=" * 60)
        print("Fix the issues above in your .env file and restart.")
        print("See .env.example for all required variables.")
        raise SystemExit(1) from exc

    # 创建必要的目录
    for dir_path in [settings.chroma_persist_dir, settings.bge_model_path,
                     os.path.dirname(settings.log_file)]:
        if dir_path and not os.path.exists(dir_path):
            os.makedirs(dir_path, exist_ok=True)

    return settings
```

### 4.2 设计要点

1. **快速失败**：配置错误时立即退出（`SystemExit(1)`），不启动应用，避免带着错误配置运行
2. **清晰错误**：逐条列出每个配置问题，直接告诉用户修复方法
3. **目录创建**：验证通过后自动创建数据目录，避免运行时 `FileNotFoundError`
4. **单一入口**：整个应用只有一处调用 `load_settings()`（在 `create_app()` 中），保证配置全局唯一

---

## 5. 命名规则

Pydantic Settings 自动将字段名转换为大写环境变量名：

| Python 字段 | 环境变量 |
|-------------|---------|
| `api_key` | `API_KEY` |
| `vector_store_type` | `VECTOR_STORE_TYPE` |
| `milvus_host` | `MILVUS_HOST` |
| `chunk_size` | `CHUNK_SIZE` |
| `agent_max_iterations` | `AGENT_MAX_ITERATIONS` |

**规则**：全大写 + 下划线分隔。无需手动映射，Pydantic 自动完成。

环境变量的**优先级**：
```
系统环境变量  >  .env 文件中的值  >  Field(default=...) 默认值
```

这意味着可以在 Docker/K8s 中通过环境变量覆盖 `.env` 文件的配置，无需修改镜像。

---

## 6. 与 Spring Boot 的对比

### 6.1 配置方式对比

| 维度 | CaraBot (Pydantic Settings) | Spring Boot |
|------|---------------------------|-------------|
| 配置文件 | `.env` 单文件 | `application.yml` / `application.properties` |
| 类型定义 | Python 类型注解 + Field() | `@ConfigurationProperties` + `@Value` |
| 验证方式 | `@field_validator` 装饰器 | `@Validated` + JSR-303 注解 |
| Profile 切换 | 多 `.env` 文件或环境变量 | `spring.profiles.active` + `application-{profile}.yml` |
| 交叉验证 | `info.data` 在验证器中访问 | `@AssertTrue` 或自定义 Validator |
| IDE 支持 | 类型提示 + docstring | Spring Boot Configuration Processor |

### 6.2 代码量对比

**Pydantic Settings（Python）**：
```python
class Settings(BaseSettings):
    api_key: str = Field(..., min_length=8)
    port: int = Field(default=8000, ge=1, le=65535)
```

**Spring Boot（Java）**：
```java
@Configuration
@ConfigurationProperties(prefix = "app")
@Validated
public class AppConfig {
    @NotBlank
    @Size(min = 8)
    private String apiKey;

    @Min(1)
    @Max(65535)
    private int port = 8000;

    // + getters & setters...
}
```

Pydantic Settings 更简洁，不需要 getter/setter 样板代码，类型即文档。

### 6.3 Profile 对比

**Spring Boot** 原生支持多环境 Profile：
```yaml
# application-dev.yml
server.port: 8080

# application-prod.yml
server.port: 80
```

**CaraBot** 通过不同 `.env` 文件实现：
```bash
# 开发环境
cp .env.example .env
# 编辑 .env，设置 VECTOR_STORE_TYPE=chroma

# 生产环境
# 通过 Docker 环境变量注入
VECTOR_STORE_TYPE=milvus
MILVUS_HOST=milvus-cluster.prod.internal
```

---

## 7. 实践：添加自定义配置

### 7.1 场景

假设我们要添加一个"上传文件类型白名单"配置项，用于限制可上传的文件格式。

### 7.2 Step 1：添加字段

在 `Settings` 类中添加：

```python
# --- Document Processing ---
allowed_mime_types: list[str] = Field(
    default=["application/pdf", "text/plain",
             "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
             "text/markdown"],
    description="Allowed MIME types for document upload",
)
```

### 7.3 Step 2：添加验证器

```python
@field_validator("allowed_mime_types")
@classmethod
def mime_types_must_be_valid(cls, v: list[str]) -> list[str]:
    """确保每个 MIME 类型包含斜杠。"""
    for mime in v:
        if "/" not in mime:
            raise ValueError(f"Invalid MIME type: '{mime}'. Must contain '/'.")
    return v
```

### 7.4 Step 3：在 .env 中配置

```bash
# 逗号分隔的列表在 Pydantic 中自动解析
ALLOWED_MIME_TYPES=["application/pdf","text/plain","text/markdown"]
```

### 7.5 Step 4：使用配置

```python
# 在 UploadService 中
if mime_type not in self._settings.allowed_mime_types:
    raise UnsupportedFileTypeException(
        f"File type '{mime_type}' is not allowed."
    )
```

---

## 8. 常见问题与调试

### 8.1 字段未生效

**现象**：修改了 `.env` 但应用仍使用旧值。

**排查**：
1. 确认环境变量是否覆盖了 `.env`：`echo $API_KEY`
2. 检查字段名是否正确：Pydantic 是大小写敏感的
3. 是否修改了错误的 `.env` 文件（工作目录不对）

### 8.2 ValidationError 排查

```
CONFIGURATION ERROR: Missing or invalid configuration values
============================================================
  [api_key] String should have at least 8 characters
  [port] Input should be less than or equal to 65535
============================================================
```

`[字段路径]` 直接指出了问题字段，按提示修改 `.env` 即可。

### 8.3 extra="forbid" 的调试价值

如果 `.env` 中写了 `API_KYE=xxx`（拼写错误），会得到：
```
Extra inputs are not permitted: API_KYE
```

这比静默跳过好得多，避免了"明明配了 API Key 却不生效"的困惑。

---

## 9. 课后作业

1. **分析现有配置**：阅读 `.env.example` 中的每一行，理解每个配置项的作用
2. **添加配置项**：为项目添加一个 `UPLOAD_RETRY_COUNT` 配置项（默认 3 次），并在 upload 服务中引用它
3. **编写验证器**：为第 2 题的配置项添加验证（范围 1-10）
4. **测试错误场景**：故意在 `.env` 中写入错误值（如 `PORT=99999`），观察 `load_settings()` 的错误输出
5. **对比 Spring Boot**：用 Java 写一个等价的配置类，对比代码量和类型安全性
6. **Profile 切换**：创建两个 `.env` 文件（开发/生产），测试配置切换流程

---

## 📚 扩展阅读
- Pydantic Settings 文档：https://docs.pydantic.dev/latest/concepts/pydantic-settings/
- Pydantic Field 文档：https://docs.pydantic.dev/latest/concepts/fields/
- Pydantic Validators：https://docs.pydantic.dev/latest/concepts/validators/
- 12-Factor App 配置原则：https://12factor.net/config
- Spring Boot 外部化配置：https://docs.spring.io/spring-boot/reference/features/external-config.html

---

**下节课预告**：第 6 课 - 分层架构设计（API 层、服务层、数据层的职责与协作）
