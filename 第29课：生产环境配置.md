# CaraBot Agent 智能知识库系统课程
## 第 29 课：生产环境配置

### 🎯 本节课目标
- 理解开发、测试、生产环境的配置隔离
- 掌握通过环境变量覆盖 .env 文件的配置管理
- 学习高可用架构的基础设计
- 学习时长：2 天
- 实践任务：配置生产环境

---

## 1. 多环境配置隔离

### 1.1 四种环境的配置差异

| 配置项 | 开发（dev） | 测试（staging） | 生产（prod） |
|--------|-----------|---------------|-------------|
| `VECTOR_STORE_TYPE` | chroma | milvus | milvus |
| `LOG_LEVEL` | DEBUG | INFO | WARNING |
| `LOG_FORMAT` | text | json | json |
| `LLM_MODEL` | gpt-4o-mini | gpt-4o-mini | gpt-4o |
| `DB_URL` | localhost | staging-db.internal | prod-db.internal |
| `REDIS_URL` | localhost | staging-redis.internal | prod-redis.internal |
| `WORKERS` | 1 | 2 | 4-8 |

### 1.2 配置文件的组织方式

```
.env.example          # 模板（提交到 Git）
.env                  # 本地开发（不提交到 Git，gitignore）
.env.staging          # 测试环境（不提交）
.env.prod             # 生产环境（不提交，由运维管理）
```

### 1.3 优先级

```
系统环境变量  >  .env 文件  >  Field(default=...) 默认值

Docker/K8s 中：
  - K8s ConfigMap/Secret → 注入为环境变量 → 最高优先级
  - 不需要修改 Docker 镜像即可切换环境
```

---

## 2. 通过 K8s 环境变量覆盖

### 2.1 K8s ConfigMap

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: carabot-config
data:
  VECTOR_STORE_TYPE: "milvus"
  LOG_LEVEL: "WARNING"
  LOG_FORMAT: "json"
  LLM_MODEL: "gpt-4o"
  MILVUS_HOST: "milvus-cluster.prod.svc.cluster.local"
  MILVUS_PORT: "19530"
  DB_URL: "postgresql+asyncpg://carabot:$(DB_PASSWORD)@prod-pg.internal:5432/carabot"
```

### 2.2 K8s Secret

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: carabot-secrets
type: Opaque
stringData:
  API_KEY: "prod-secret-key-at-least-32-characters"
  LLM_API_KEY: "sk-prod-xxxx"
  DB_PASSWORD: "secure-db-password"
```

### 2.3 K8s Deployment

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: carabot
spec:
  replicas: 3  # 3 个副本实现高可用
  template:
    spec:
      containers:
      - name: carabot
        image: carabot:latest
        envFrom:
        - configMapRef:
            name: carabot-config
        - secretRef:
            name: carabot-secrets
        resources:
          requests:
            cpu: "500m"
            memory: "512Mi"
          limits:
            cpu: "2"
            memory: "2Gi"
```

---

## 3. 高可用架构基础

### 3.1 单点故障分析

| 组件 | 单点？ | 高可用方案 |
|------|--------|----------|
| CaraBot (API) | 是（单实例） | K8s Deployment replicas=3 + Service 负载均衡 |
| PostgreSQL | 是 | Patroni/Cloud SQL（主从 + 自动故障转移） |
| Redis | 是 | Redis Sentinel / Cluster |
| Milvus | 分布式架构 | 多 Data Node + 多 Index Node |

### 3.2 高可用拓扑

```
          ┌─────────────┐
          │  K8s Ingress │
          └──────┬──────┘
                 │
    ┌────────────┼────────────┐
    ▼            ▼            ▼
┌───────┐  ┌───────┐  ┌───────┐
│CaraBot│  │CaraBot│  │CaraBot│  ← 3 个副本
│ Pod 1 │  │ Pod 2 │  │ Pod 3 │
└───┬───┘  └───┬───┘  └───┬───┘
    └──────────┼──────────┘
               │
    ┌──────────┼──────────┐
    ▼          ▼          ▼
┌───────┐ ┌───────┐ ┌──────────┐
│  PG   │ │ Redis │ │  Milvus  │
│Primary│ │Sentinel│ │ Cluster  │
│+Replica│ │       │ │          │
└───────┘ └───────┘ └──────────┘
```

---

## 4. 启动检查清单

生产环境上线前确认：

- [ ] `.env` 检查：无默认值（`change-me`、`your-api-key-here`）
- [ ] `LOG_LEVEL=WARNING`（生产不开 DEBUG）
- [ ] `LOG_FORMAT=json`
- [ ] `VECTOR_STORE_TYPE=milvus`（生产不用 Chroma）
- [ ] `WORKERS` 根据 CPU 核数设置（通常 2-8）
- [ ] API_KEY 长度 ≥ 32 字符
- [ ] 所有外部服务（DB、Redis、Milvus）健康检查通过
- [ ] 健康检查端点 `/health` 返回 200
- [ ] HTTPS 已启用（通过反向代理或 K8s Ingress）

---

## 5. 课后作业

1. **创建多环境配置**：创建 `.env.dev`、`.env.staging`、`.env.prod` 三个文件，差异化的配置项至少 10 个
2. **写 K8s 配置**：为 CaraBot 编写完整的 K8s Deployment + Service + ConfigMap + Secret YAML
3. **环境切换测试**：分别用开发和生产的 .env 启动 CaraBot，验证配置生效
4. **资源限制测试**：在 Docker 中限制 CPU 和内存，模拟生产资源约束下的表现
5. **高可用架构图**：画出完整的 CaraBot 生产部署架构图，标注所有组件和数据流

---

## 📚 扩展阅读
- 12-Factor App 配置：https://12factor.net/config
- K8s ConfigMap：https://kubernetes.io/docs/concepts/configuration/configmap/
- K8s Secrets：https://kubernetes.io/docs/concepts/configuration/secret/

---

**下节课预告**：第 30 课 - 监控与告警
