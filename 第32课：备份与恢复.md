# CaraBot Agent 智能知识库系统课程
## 第 32 课：备份与恢复

### 🎯 本节课目标
- 理解 CaraBot 的四类数据及其备份策略
- 掌握 PostgreSQL 逻辑备份与增量备份
- 学习向量数据备份和 Checkpoint 恢复
- 制定灾难恢复演练计划
- 学习时长：1 天
- 实践任务：制定备份恢复计划

---

## 1. 四类数据，四种备份策略

| 数据 | 存储位置 | 丢失后果 | 备份优先级 | 备份方式 |
|------|---------|---------|----------|---------|
| **文档元数据** | PostgreSQL `documents` 表 | 无法知道有哪些文档 | 🔴 高 | pg_dump / WAL 归档 |
| **向量数据** | Milvus / Chroma 文件 | 检索功能丧失 | 🔴 高 | Milvus backup / 文件拷贝 |
| **对话历史** | PostgreSQL `checkpoints` 表 | 用户对话状态丢失 | 🟡 中 | pg_dump / WAL 归档 |
| **应用日志** | `./logs/carabot.log` | 排查历史问题困难 | 🟢 低 | 日志归档（ELK） |
| **模型文件** | `./data/models/bge-m3/` | 需重新下载（可恢复） | 🟢 低 | 不需备份（重新下载） |

---

## 2. PostgreSQL 备份

### 2.1 逻辑备份（pg_dump）

```bash
# 完整备份
pg_dump -h localhost -U carabot -d carabot \
  --format=custom \
  --file=backup_$(date +%Y%m%d_%H%M%S).dump

# 只备份文档元数据（不含 checkpoints）
pg_dump -h localhost -U carabot -d carabot \
  --table=documents \
  --data-only \
  --file=documents_backup.sql

# 定时备份（crontab）
0 2 * * * /usr/local/bin/backup_carabot.sh  # 每天凌晨 2 点
```

### 2.2 备份脚本

```bash
#!/bin/bash
# backup_carabot.sh

BACKUP_DIR="/backups/carabot"
DB_HOST="localhost"
DB_USER="carabot"
DB_NAME="carabot"
RETENTION_DAYS=30

TIMESTAMP=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/carabot_$TIMESTAMP.dump"

# 执行备份
pg_dump -h "$DB_HOST" -U "$DB_USER" -d "$DB_NAME" \
  --format=custom --file="$BACKUP_FILE"

# 压缩
gzip "$BACKUP_FILE"

# 清理 30 天前的备份
find "$BACKUP_DIR" -name "*.dump.gz" -mtime +$RETENTION_DAYS -delete

echo "Backup completed: ${BACKUP_FILE}.gz"
```

### 2.3 恢复

```bash
# 恢复完整数据库
pg_restore -h localhost -U carabot -d carabot \
  --clean --if-exists \
  backup_20240315_020000.dump.gz

# 只恢复 documents 表
pg_restore -h localhost -U carabot -d carabot \
  --table=documents --data-only \
  backup_20240315_020000.dump.gz
```

---

## 3. 向量数据备份

### 3.1 Milvus 备份

```bash
# Milvus 提供实验性的备份工具
# https://milvus.io/docs/backup_and_restore.md

# 方式一：Milvus Backup CLI
milvus-backup create -n carabot_knowledge_backup

# 方式二：直接备份 MinIO 中的对象存储数据
# Milvus 的数据存储在 MinIO/S3 中
aws s3 sync s3://milvus-bucket/ /backups/milvus/ --endpoint-url=http://minio:9000
```

### 3.2 Chroma 备份

```bash
# Chroma 数据是本地文件，直接拷贝即可
tar -czf chroma_backup_$(date +%Y%m%d).tar.gz ./data/chroma/

# 恢复
tar -xzf chroma_backup_20240315.tar.gz
```

---

## 4. 灾难恢复演练

### 4.1 恢复流程（RTO：Recovery Time Objective）

```
Phase 1: 基础设施恢复（预计 30 分钟）
  ├── 启动 PostgreSQL + Redis + Milvus
  ├── 验证各组件健康检查
  └── 创建必要的数据库和用户

Phase 2: 数据恢复（预计 20 分钟）
  ├── pg_restore 恢复文档元数据
  ├── 恢复 Milvus 向量数据
  └── 验证数据完整性（SELECT COUNT(*) vs 备份时的记录数）

Phase 3: 应用恢复（预计 10 分钟）
  ├── 部署 CaraBot（Docker / K8s）
  ├── 配置环境变量指向恢复后的数据库
  └── 验证 /health 端点

总 RTO: ~60 分钟
```

### 4.2 RPO（Recovery Point Objective）

| 数据 | RPO | 备份频率 |
|------|-----|---------|
| 文档元数据 | < 24 小时 | 每天凌晨全量 + WAL 持续归档 |
| 向量数据 | < 24 小时 | 每天凌晨 |
| 对话历史 | < 7 天 | 每周一次（可接受丢失） |
| 模型文件 | 可从 HF 下载 | 不需要备份 |

### 4.3 恢复清单

```markdown
# 灾难恢复检查清单

- [ ] 确认备份文件存在于备份服务器
- [ ] 检查最近一次备份的时间戳
- [ ] 恢复 PostgreSQL（按恢复流程）
- [ ] 验证 documents 表记录数
- [ ] 恢复 Milvus（或重建索引）
- [ ] 验证搜索返回正确结果
- [ ] 重启 CaraBot 服务
- [ ] 验证 /health 端点正常
- [ ] 执行 5 个典型查询验证功能
- [ ] 记录恢复耗时和遇到的问题
```

---

## 5. 课后作业

1. **编写备份脚本**：为 CaraBot 编写自动化备份脚本（PostgreSQL + Chroma 文件）
2. **测试恢复**：故意删除几条文档记录，演练从备份恢复的过程
3. **计算 RPO/RTO**：基于你的备份策略，计算数据丢失窗口（RPO）和恢复时间（RTO）
4. **Milvus 备份探索**：研究 Milvus 的备份与恢复功能，输出一份操作指南
5. **制定运维手册**：编写完整的 CaraBot 备份与恢复运维手册

---

## 📚 扩展阅读
- PostgreSQL 备份文档：https://www.postgresql.org/docs/current/backup.html
- Milvus 备份恢复：https://milvus.io/docs/backup_and_restore.md
- 3-2-1 备份策略：https://www.backblaze.com/blog/the-3-2-1-backup-strategy/

---

**下节课预告**：第 33 课 - 项目实战与总结
