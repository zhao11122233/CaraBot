# CaraBot 企业级 RAG 系统课程
## 第 3 课：项目结构与代码组织

### 🎯 本节课目标
- 理解项目的整体架构和代码组织
- 掌握核心文件的作用和职责
- 对比 Python 项目与 Java 项目的差异
- 学习时长：1 天
- 实践任务：绘制项目架构图

---

## 1. 项目整体结构

### 1.1 完整目录结构

```
CaraBot/
├── data/                 # 数据目录
│   ├── chroma/           # Chroma 向量存储数据
│   ├── chroma_v2/        # Chroma v2 版本数据
│   └── carabot.db        # SQLite 数据库（开发环境）
├── logs/                 # 日志目录
│   └── carabot.log       # 应用日志
├── scripts/              # 辅助脚本
│   ├── evaluate.py       # RAG 效果评估脚本
│   └── init_db.py        # 数据库初始化脚本
├── src/                  # 主代码目录
│   └── app/              # 应用核心
│       ├── api/          # API 层
│       │   └── routes/   # 具体路由实现
│       ├── core/         # 核心配置和工具
│       ├── middleware/   # 中间件（认证、日志、错误处理）
│       ├── models/       # 数据模型
│       ├── services/     # 业务逻辑层
│       └── vectorstore/  # 向量存储抽象层
├── tests/                # 测试代码
├── .env.example          # 环境变量模板
├── Dockerfile            # Docker 构建文件
├── README.md             # 项目说明
├── requirements.txt      # Python 依赖
└── docker-compose.yml    # Docker Compose 配置
```

### 1.2 目录结构说明

#### 1.2.1 数据目录（data/）
- 用于存储项目运行时产生的数据
- **chroma/**：Chroma 向量存储的数据文件
- **chroma_v2/**：Chroma v2 版本的数据文件
- **carabot.db**：SQLite 数据库文件（仅用于开发环境）

#### 1.2.2 日志目录（logs/）
- 用于存储项目运行时产生的日志
- **carabot.log**：应用日志文件，包含请求日志、错误日志等

#### 1.2.3 辅助脚本目录（scripts/）
- 用于存储辅助脚本，如数据库初始化、效果评估等
- **evaluate.py**：RAG 效果评估脚本，用于评估检索结果的质量
- **init_db.py**：数据库初始化脚本，用于创建表结构和初始化数据

#### 1.2.4 主代码目录（src/）
- 用于存储项目的主代码
- **app/**：应用核心，包含所有业务逻辑和 API 实现

#### 1.2.5 测试代码目录（tests/）
- 用于存储测试代码，包括单元测试、集成测试等

#### 1.2.6 配置文件
- **.env.example**：环境变量模板，包含所有配置项的说明
- **Dockerfile**：Docker 构建文件，用于构建 Docker 镜像
- **README.md**：项目说明文件，包含项目介绍、快速开始、技术栈等
- **requirements.txt**：Python 依赖文件，包含所有需要安装的第三方库
- **docker-compose.yml**：Docker Compose 配置文件，用于启动基础设施服务（如数据库、Redis、Milvus 等）

---

## 2. 核心模块说明

### 2.1 API 层（src/app/api/）

#### 2.1.1 职责
- 处理 HTTP 请求和响应
- 路由分发，将请求转发到对应的业务逻辑
- 参数验证和数据序列化

#### 2.1.2 目录结构
```
api/
├── routes/               # 具体路由实现
│   ├── __init__.py       # 路由模块初始化
│   ├── health.py         # 健康检查路由
│   ├── search.py         # 文档搜索路由
│   ├── stats.py          # 统计信息路由
│   └── upload.py         # 文档上传路由
└── __init__.py           # API 层初始化
```

#### 2.1.3 关键文件说明
- **routes/health.py**：处理健康检查请求，返回系统状态
- **routes/upload.py**：处理文档上传请求，将文档存入向量数据库
- **routes/search.py**：处理文档搜索请求，从向量数据库中检索相关文档
- **routes/stats.py**：处理统计信息请求，返回系统运行统计数据

#### 2.1.4 路由实现示例
```python
from fastapi import APIRouter, Depends, File, UploadFile
from src.app.services.upload_service import UploadService, get_upload_service
from src.app.models.schemas import UploadResponse

router = APIRouter()

@router.post("/upload", response_model=UploadResponse)
async def upload_file(
    file: UploadFile = File(...),
    author: str | None = None,
    collection: str | None = None,
    upload_service: UploadService = Depends(get_upload_service),
) -> UploadResponse:
    """Upload and index a document."""
    return await upload_service.upload(
        await file.read(),
        file.filename,
        file.content_type,
        author,
        collection,
    )
```

### 2.2 核心配置和工具（src/app/core/）

#### 2.2.1 职责
- 管理应用配置
- 提供通用工具函数
- 处理日志和异常

#### 2.2.2 目录结构
```
core/
├── __init__.py           # 核心模块初始化
├── config.py             # 应用配置
├── exceptions.py         # 自定义异常
└── logging.py            # 日志配置
```

#### 2.2.3 关键文件说明
- **config.py**：管理应用配置，从环境变量或 .env 文件加载配置
- **exceptions.py**：定义自定义异常类，用于业务逻辑中的错误处理
- **logging.py**：配置日志系统，生成结构化 JSON 日志

#### 2.2.4 配置实现示例
```python
from pydantic_settings import BaseSettings, SettingsConfigDict

class Settings(BaseSettings):
    """Application settings."""
    model_config = SettingsConfigDict(env_file=".env")

    api_key: str
    vector_store_type: str = "chroma"
    db_url: str = "postgresql+asyncpg://carabot:carabot@localhost:5432/carabot"
    redis_url: str = "redis://localhost:6379/0"
    bge_model_name: str = "BAAI/bge-m3"
    bge_device: str = "cpu"

settings = Settings()
```

### 2.3 中间件（src/app/middleware/）

#### 2.3.1 职责
- 处理 HTTP 请求的前置和后置逻辑
- 实现认证、日志、错误处理等横切关注点

#### 2.3.2 目录结构
```
middleware/
├── __init__.py           # 中间件模块初始化
├── auth.py               # 认证中间件
├── error_handler.py      # 错误处理中间件
└── logging.py            # 日志中间件
```

#### 2.3.3 关键文件说明
- **auth.py**：实现 API 密钥认证，验证请求头中的 X-API-Key
- **logging.py**：实现请求日志记录，记录每个请求的详细信息
- **error_handler.py**：实现全局错误处理，将异常转换为友好的响应

#### 2.3.4 中间件实现示例
```python
from fastapi import Request, HTTPException
from starlette.middleware.base import BaseHTTPMiddleware
from src.app.core.config import settings

class AuthGuard(BaseHTTPMiddleware):
    """API key authentication middleware."""

    async def dispatch(self, request: Request, call_next):
        """Dispatch request with authentication."""
        # 跳过健康检查接口的认证
        if request.url.path == "/health":
            return await call_next(request)

        # 获取 API 密钥
        api_key = request.headers.get("X-API-Key")
        if not api_key:
            raise HTTPException(status_code=401, detail="X-API-Key header is missing")

        # 验证 API 密钥
        if api_key != settings.api_key:
            raise HTTPException(status_code=401, detail="Invalid API key")

        # 继续处理请求
        response = await call_next(request)
        return response
```

### 2.4 数据模型（src/app/models/）

#### 2.4.1 职责
- 定义数据库模型
- 定义 API 请求和响应模型
- 实现数据验证和序列化

#### 2.4.2 目录结构
```
models/
├── __init__.py           # 模型模块初始化
├── document.py           # 数据库模型
└── schemas.py            # API 模型
```

#### 2.4.3 关键文件说明
- **document.py**：定义数据库模型，使用 SQLAlchemy ORM
- **schemas.py**：定义 API 请求和响应模型，使用 Pydantic

#### 2.4.4 模型实现示例
```python
# 数据库模型示例
from sqlalchemy import Column, String, DateTime, Boolean
from sqlalchemy.ext.declarative import declarative_base
from datetime import datetime

Base = declarative_base()

class Document(Base):
    """Document model."""
    __tablename__ = "documents"

    id = Column(String, primary_key=True, index=True)
    filename = Column(String, index=True)
    file_hash = Column(String, unique=True, index=True)
    file_size = Column(Integer)
    chunk_count = Column(Integer)
    author = Column(String, nullable=True)
    collection = Column(String, nullable=True)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    is_deleted = Column(Boolean, default=False)
```

```python
# API 模型示例
from pydantic import BaseModel
from datetime import datetime

class UploadResponse(BaseModel):
    """Upload response model."""
    document_id: str
    filename: str
    file_hash: str
    file_size: int
    chunk_count: int
    is_duplicate: bool
    message: str

    class Config:
        """Pydantic config."""
        orm_mode = True
```

### 2.5 业务逻辑层（src/app/services/）

#### 2.5.1 职责
- 实现核心业务逻辑
- 协调各个模块的工作
- 处理复杂的业务流程

#### 2.5.2 目录结构
```
services/
├── __init__.py           # 服务模块初始化
├── dedup_service.py      # 文档去重服务
├── document_processor.py # 文档处理服务
├── embedding_service.py  # 向量嵌入服务
├── search_service.py     # 文档搜索服务
├── stats_service.py      # 统计信息服务
└── upload_service.py     # 文档上传服务
```

#### 2.5.3 关键文件说明
- **upload_service.py**：处理文档上传的完整流程，包括格式解析、文本分块、向量嵌入、存储等
- **search_service.py**：处理文档搜索的完整流程，包括问题向量化、向量检索、结果排序等
- **embedding_service.py**：处理文本向量嵌入，使用预训练模型将文本转换为向量
- **dedup_service.py**：处理文档去重，基于文件哈希判断是否重复
- **document_processor.py**：处理文档解析和分块，支持多种格式（PDF、DOCX、Markdown 等）

#### 2.5.4 服务实现示例
```python
from src.app.core.config import Settings
from src.app.services.document_processor import DocumentProcessor
from src.app.services.embedding_service import EmbeddingService
from src.app.vectorstore.base import BaseVectorStore
from src.app.services.dedup_service import DedupService

class UploadService:
    """Upload orchestration service."""

    def __init__(
        self,
        settings: Settings,
        doc_processor: DocumentProcessor,
        embedding_service: EmbeddingService,
        vector_store: BaseVectorStore,
        dedup_service: DedupService,
    ) -> None:
        self._settings = settings
        self._doc_processor = doc_processor
        self._embedding_service = embedding_service
        self._vector_store = vector_store
        self._dedup_service = dedup_service

    async def upload(
        self,
        file_content: bytes,
        filename: str,
        mime_type: str | None = None,
        author: str | None = None,
        collection: str | None = None,
    ) -> dict:
        """Process and index a single uploaded document."""
        # 实现文档上传逻辑
        pass
```

### 2.6 向量存储抽象层（src/app/vectorstore/）

#### 2.6.1 职责
- 抽象向量存储操作
- 支持多种向量存储后端（如 Milvus、Chroma）
- 提供统一的 API 接口

#### 2.6.2 目录结构
```
vectorstore/
├── __init__.py           # 向量存储模块初始化
├── base.py               # 抽象基类
├── chroma_store.py       # Chroma 向量存储实现
└── milvus_store.py       # Milvus 向量存储实现
```

#### 2.6.3 关键文件说明
- **base.py**：定义向量存储抽象基类，所有向量存储实现都必须继承这个类
- **chroma_store.py**：Chroma 向量存储的具体实现
- **milvus_store.py**：Milvus 向量存储的具体实现

#### 2.6.4 抽象基类示例
```python
from abc import ABC, abstractmethod
from langchain_core.documents import Document

class BaseVectorStore(ABC):
    """Abstract base class for vector stores."""

    @abstractmethod
    async def add_documents(
        self,
        documents: list[Document],
        collection: str | None = None,
        embeddings: list[list[float]] | None = None,
    ) -> list[str]:
        """Add documents to vector store."""
        ...

    @abstractmethod
    async def search(
        self,
        query_embedding: list[float],
        top_k: int = 5,
        collection: str | None = None,
        threshold: float | None = None,
    ) -> list[tuple[Document, float]]:
        """Search similar documents."""
        ...

    @abstractmethod
    async def delete(
        self,
        doc_ids: list[str],
        collection: str | None = None,
    ) -> int:
        """Delete documents from vector store."""
        ...
```

---

## 3. 与 Java 项目的对比

### 3.1 项目结构对比

| Python 项目结构 | Java 项目结构 | 说明 |
|----------------|--------------|------|
| src/app/api/   | src/main/java/com/example/controller/ | API 层，处理 HTTP 请求 |
| src/app/services/ | src/main/java/com/example/service/ | 业务逻辑层，实现核心业务 |
| src/app/models/ | src/main/java/com/example/model/ | 数据模型，包括数据库模型和 API 模型 |
| src/app/core/ | src/main/java/com/example/config/ | 核心配置和工具 |
| requirements.txt | pom.xml / build.gradle | 依赖管理 |
| .env | application.properties / application.yml | 环境变量配置 |

### 3.2 代码风格对比

#### Python 代码风格
```python
# 函数式风格，简洁直观
from fastapi import APIRouter

router = APIRouter()

@router.get("/health")
async def health_check():
    """Health check endpoint."""
    return {"status": "healthy"}
```

#### Java 代码风格
```java
// 面向对象风格，结构严谨
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class HealthController {
    
    @GetMapping("/health")
    public HealthResponse healthCheck() {
        HealthResponse response = new HealthResponse();
        response.setStatus("healthy");
        return response;
    }
    
    static class HealthResponse {
        private String status;
        
        // getter 和 setter
        public String getStatus() {
            return status;
        }
        
        public void setStatus(String status) {
            this.status = status;
        }
    }
}
```

### 3.3 开发效率对比

| 特性 | Python | Java |
|------|--------|------|
| 开发速度 | 快，代码简洁，无需编译 | 较慢，代码冗长，需要编译 |
| 运行速度 | 较慢，解释执行 | 快，编译执行 |
| 类型安全 | 动态类型，运行时检查 | 静态类型，编译时检查 |
| 生态系统 | 丰富，第三方库多 | 成熟，企业级应用多 |
| 学习曲线 | 平缓，容易上手 | 陡峭，需要掌握大量概念 |

---

## 4. 项目架构设计原则

### 4.1 分层架构原则
- **单一职责**：每个模块只负责一个方面的功能
- **依赖倒置**：高层模块不依赖低层模块，两者都依赖抽象
- **接口隔离**：客户端不应该依赖它不需要的接口
- **迪米特法则**：一个对象应该对其他对象有尽可能少的了解

### 4.2 模块化设计原则
- **高内聚**：模块内部的元素应该紧密相关，共同完成一个单一的功能
- **低耦合**：模块之间的依赖应该尽可能少，接口应该尽可能简单
- **可复用**：模块应该可以在不同的场景中复用
- **可扩展**：模块应该可以轻松扩展新的功能，而不需要修改现有代码

### 4.3 代码组织原则
- **清晰的命名**：文件和函数的命名应该清晰明了，能够表达其用途
- **合理的注释**：关键代码应该有注释，解释其实现思路和业务逻辑
- **一致的风格**：代码风格应该一致，包括缩进、换行、变量命名等
- **适当的粒度**：函数和类的大小应该适当，不应该过大或过小

---

## 5. 课后作业

1. **绘制项目架构图**：根据项目结构，绘制项目架构图，包括各个模块之间的依赖关系
答案见模拟面经.md/问题十
2. **分析核心文件**：选择 3-5 个核心文件，分析其作用和实现思路 1.dockerfile作用：定义了项目的构建，依赖安装，运行环境，是系统能跑起来的基础；实现思路：采用 builder 编译阶段 + runtime 运行阶段分离：
builder 阶段：装 gcc、编译依赖、pip 安装所有包到 /install。
runtime 阶段：只带最小 Python 镜像 + 运行时依赖（libgomp1、curl），从 builder 复制已装好的依赖到 /usr/local。
安全：用非 root 用户 carabot 运行。
健康检查：curl /health 保证服务可用。
2.requirements.txt作用：列出了项目所有python包，包括核心技术栈实现思路
按功能分组，只列必要依赖，版本严格锁定：
Web：fastapi、uvicorn
RAG 核心：langchain、langchain-community、langchain-text-splitters
向量：pymilvus /chromadb、sentence-transformers（BGE-M3）
文档解析：pypdf、pdfplumber、python-docx、docx2txt
数据库：sqlalchemy、asyncpg、redis
工具：python-dotenv、pydantic-settings
3.src/app/main.py作用：fastapi的应用的入口文件包括路由注册，中间件配置，应用启动与初始化逻辑实现思路（FastAPI 分层架构）
创建 FastAPI 实例 → 全局中间件（日志、CORS、异常捕获）
注册路由：
/api/v1/docs → 文档上传 / 管理
/api/v1/query → 知识库检索
/api/v1/health → 健康检查
依赖注入：统一管理 db、redis、向量库连接
生命周期：启动时初始化 embedding 模型、连接向量库；关闭时释放连接。
4.src/services/retrieval_service.py作用：实现了企业级智能检索的核心流程，接受用户问题->调用向量数据库转向量->向量数据库检索->结果排序->返回答案5.src/services/document_service.py作用：解析markdown/pdf/txt文件->文档切块->去重—>存入向量库/关系库
3. **对比 Java 项目**：对比 Python 项目与 Java 项目的差异，列出至少 5 个不同点：python代码简单，直观，不用编译，Java项目复杂，需要编译；python运行时间长，java运行时间短；python有丰富的第三方库，java有成熟的企业级应用；python动态类型运行时检查，Java静态类型，编译时检查；python学习平缓容易上手，Java技术栈陡峭，需要掌握大量概念。
4. **优化建议**：根据你的分析，提出至少 3 个项目优化建议
    建议：优化数据库查询，减少查询次数，提高查询效率；优化向量数据库索引，提高查找效率；优化文档解析，减少解析次数，提高解析效率。
5. **学习笔记**：记录学习过程中的疑问和收获

---

## 📚 扩展阅读
- FastAPI 官方文档：https://fastapi.tiangolo.com/
- SQLAlchemy 官方文档：https://www.sqlalchemy.org/
- Pydantic 官方文档：https://docs.pydantic.dev/
- Python 代码风格指南：https://peps.python.org/pep-0008/

---

**下节课预告**：第 4 课 - FastAPI 框架基础