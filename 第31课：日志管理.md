# CaraBot Agent 智能知识库系统课程
## 第 31 课：日志管理

### 🎯 本节课目标
- 理解 ELK（Elasticsearch + Logstash + Kibana）日志栈的架构
- 掌握 JSON 日志的采集、索引和分析
- 学习日志轮转与清理策略
- 学习时长：1 天
- 实践任务：分析应用日志

---

## 1. ELK 日志栈架构

### 1.1 架构图

```
┌────────┐    ┌──────────┐    ┌─────────────┐    ┌────────┐
│ CaraBot│───→│ Filebeat │───→│ Logstash     │───→│Elastic-│
│ (JSON  │    │ (采集)    │    │ (过滤/转换)   │    │search  │
│  logs) │    └──────────┘    └─────────────┘    │(存储)  │
└────────┘                                       └───┬────┘
                                                     │
                                               ┌─────▼────┐
                                               │  Kibana  │
                                               │ (可视化)  │
                                               └──────────┘
```

### 1.2 各组件职责

| 组件 | 职责 | 替代方案 |
|------|------|---------|
| **Filebeat** | 读取日志文件，发送到 Logstash | Fluentd, Vector |
| **Logstash** | 解析、过滤、转换日志 | 可跳过（Filebeat 直接写 ES） |
| **Elasticsearch** | 存储和索引日志，全文搜索 | Loki（Grafana 生态） |
| **Kibana** | 日志搜索 UI、仪表板 | Grafana（也可查 ES） |

---

## 2. CaraBot JSON 日志 → ELK

### 2.1 CaraBot 生成的日志格式

```json
{
  "timestamp": "2024-03-15T10:30:45.123Z",
  "level": "ERROR",
  "logger": "src.app.services.search_service",
  "module": "search_service:45",
  "message": "Embedding failed: CUDA out of memory",
  "trace_id": "a1b2c3d4-e5f6-...",
  "exception": "Traceback (most recent call last):\n  File ..."
}
```

### 2.2 Filebeat 配置

```yaml
# filebeat.yml
filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /app/logs/carabot.log
    json.keys_under_root: true     # JSON 字段直接作为顶层字段
    json.add_error_key: true       # 解析失败时加 error.message 字段

output.elasticsearch:
  hosts: ["elasticsearch:9200"]
  index: "carabot-logs-%{+yyyy.MM.dd}"  # 按天分索引

# 或者输出到 Logstash
# output.logstash:
#   hosts: ["logstash:5044"]
```

### 2.3 Kibana 常用查询

```
# 查某个 trace_id 的所有日志
trace_id: "a1b2c3d4-e5f6-..."

# 查所有 ERROR 级别日志
level: "ERROR"

# 查某个服务的日志
logger: "src.app.services.agent_service"

# 查慢搜索（> 200ms）
logger: "src.app.services.search_service" AND message: *time=???ms*

# 查某个时间范围的错误
level: "ERROR" AND @timestamp: [2024-03-15T10:00:00 TO 2024-03-15T11:00:00]
```

---

## 3. 日志轮转与清理

### 3.1 Python logging 的 RotatingFileHandler

```python
from logging.handlers import RotatingFileHandler

file_handler = RotatingFileHandler(
    settings.log_file,
    maxBytes=50 * 1024 * 1024,   # 50MB 一个文件
    backupCount=10,              # 保留 10 个旧文件
    encoding="utf-8",
)
# 文件会变成：carabot.log, carabot.log.1, carabot.log.2, ...
```

### 3.2 日志保留策略

| 环境 | 保留时间 | 原因 |
|------|---------|------|
| 开发 | 3 天 | 磁盘空间有限 |
| 测试 | 7 天 | 需要一定时间分析问题 |
| 生产 | 30 天（ES 热数据）+ 90 天（归档） | 合规和问题追溯 |

### 3.3 Elasticsearch 索引生命周期

```json
// ILM Policy（索引生命周期管理）
{
  "policy": {
    "phases": {
      "hot": {
        "actions": { "rollover": { "max_size": "50GB", "max_age": "1d" } }
      },
      "warm": {
        "min_age": "7d",
        "actions": { "shrink": { "number_of_shards": 1 } }
      },
      "delete": {
        "min_age": "30d",
        "actions": { "delete": {} }
      }
    }
  }
}
```

---

## 4. 日志分析的最佳实践

### 4.1 结构化日志原则

```python
# ✅ 好：字段化关键信息
logger.info(
    "Upload complete: doc_id=%s, file='%s', chunks=%d, size=%d, incremental=%s",
    doc_id, filename, chunk_count, file_size, is_incremental,
)

# ❌ 差：自然语言堆砌
logger.info(f"文件 {filename} 已经上传完成了，分了 {chunk_count} 块")

# 在 ELK 中：
# ✅ 可以用正则提取 doc_id, chunks, size 等字段做聚合
# ❌ 无法结构化解析
```

### 4.2 日志级别使用分布监控

```
正常系统的日志级别分布（预期）：
  DEBUG:   15%
  INFO:    75%
  WARNING: 8%
  ERROR:   2%

异常信号：
  ERROR > 5%    → 系统有持续性问题
  WARNING > 15% → 有退化趋势
  DEBUG 被关掉   → 无法有效排查问题
```

---

## 5. 课后作业

1. **配置日志轮转**：在 CaraBot 中添加 `RotatingFileHandler`（50MB/10个备份）
2. **搭建 ELK**：用 Docker Compose 启动 Elasticsearch + Kibana + Filebeat，采集 CaraBot 日志
3. **创建 Kibana Dashboard**：展示日志级别分布、按服务的日志量、Top 10 错误信息
4. **日志分析脚本**：用 Python + jq 写一个脚本，统计最近 1 小时内各接口的请求量、平均耗时、错误数
5. **trace_id 追踪**：在 Kibana 中按 trace_id 搜索，验证能否看到完整请求链路

---

## 📚 扩展阅读
- Filebeat 文档：https://www.elastic.co/guide/en/beats/filebeat/current/
- Elasticsearch 文档：https://www.elastic.co/guide/en/elasticsearch/reference/current/
- Python logging 指南：https://docs.python.org/3/howto/logging.html

---

**下节课预告**：第 32 课 - 备份与恢复
