version: "3.8"

services:
  # --- Milvus Vector Database ---
  etcd:
    image: quay.io/coreos/etcd:v3.5.5
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    volumes:
      - etcd_data:/etcd
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - carabot-net

  minio:
    image: minio/minio:RELEASE.2023-03-20T20-16-18Z
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    volumes:
      - minio_data:/minio_data
    command: minio server /minio_data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - carabot-net

  milvus-standalone:
    image: milvusdb/milvus:v2.4.0
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
    volumes:
      - milvus_data:/var/lib/milvus
    ports:
      - "19530:19530"
      - "9091:9091"
    depends_on:
      etcd:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9091/healthz"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - carabot-net

  # --- PostgreSQL ---
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: carabot
      POSTGRES_PASSWORD: carabot
      POSTGRES_DB: carabot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U carabot"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - carabot-net

  # --- Redis ---
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - carabot-net

  # --- CaraBot Application ---
  carabot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY:-carabot-secret-key-change-me}
      - VECTOR_STORE_TYPE=milvus
      - MILVUS_HOST=milvus-standalone
      - MILVUS_PORT=19530
      - DB_URL=postgresql+asyncpg://carabot:carabot@postgres:5432/carabot
      - REDIS_URL=redis://redis:6379/0
      - BGE_MODEL_PATH=/app/data/models/bge-m3
      - LOG_LEVEL=INFO
      - LOG_FILE=/app/logs/carabot.log
    volumes:
      - carabot_data:/app/data
      - carabot_logs:/app/logs
    depends_on:
      milvus-standalone:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - carabot-net

volumes:
  etcd_data:
  minio_data:
  milvus_data:
  postgres_data:
  redis_data:
  carabot_data:
  carabot_logs:

networks:
  carabot-net:
    driver: bridge
