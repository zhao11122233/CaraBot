# CaraBot Agent 智能知识库系统课程
## 第 23 课：多格式文档支持

### 🎯 本节课目标
- 理解 CaraBot 的可扩展文档格式架构
- 掌握如何在 DocumentProcessor 中添加新的文档格式
- 学习 MIME 类型检测和 Loader 路由模式
- 学习时长：2 天
- 实践任务：添加新的文档格式支持

---

## 1. CaraBot 当前支持的格式

| 格式 | MIME 类型 | Loader | 备注 |
|------|----------|--------|------|
| PDF | `application/pdf` | `PyPDFLoader` | 每页一个 Document |
| DOCX | `application/vnd...wordprocessingml.document` | `Docx2txtLoader` | 纯文本提取 |
| DOC | `application/msword` | `Docx2txtLoader` | 旧格式 |
| Markdown | `text/markdown` | `UnstructuredMarkdownLoader` | 有后备 TextLoader |
| TXT | `text/plain` | `TextLoader` | 最简单 |

---

## 2. 可扩展架构

### 2.1 添加新格式只需改四个地方

```
❶ SUPPORTED_MIMETYPES 字典：注册 MIME → 扩展名
❷ SUPPORTED_EXTENSIONS 集合：注册支持的扩展名
❸ _infer_mime_type() 方法：扩展名 → MIME 的逆向映射
❹ _load_file() 方法：MIME → Loader 的分支
```

### 2.2 实战：添加 HTML 支持

**Step 1：注册 MIME 类型**
```python
SUPPORTED_MIMETYPES = {
    # ... 已有的
    "text/html": ".html",
}
SUPPORTED_EXTENSIONS = {".pdf", ".docx", ".doc", ".md", ".txt", ".markdown", ".html"}
```

**Step 2：扩展名映射**
```python
def _infer_mime_type(self, filename: str) -> str:
    mapping = {
        # ... 已有的
        ".html": "text/html",
        ".htm": "text/html",
    }
```

**Step 3：添加 Loader 分支**
```python
elif mime_type == "text/html":
    from langchain_community.document_loaders import UnstructuredHTMLLoader
    loader = UnstructuredHTMLLoader(file_path)
    return loader.load()
```

**Step 4：测试**
```bash
curl -X POST http://localhost:8000/api/v1/upload \
  -H "X-API-Key: carabot-secret" \
  -F "file=@index.html"
```

---

## 3. 格式扩展的最佳实践

### 3.1 Loader 选择原则

| 场景 | 推荐 Loader | 原因 |
|------|-----------|------|
| 结构化文档（PDF/Word） | 专用 Loader | 能正确提取内容 |
| 简单文本（TXT/MD） | `TextLoader` | 够用 |
| 复杂格式（HTML/EPUB） | `Unstructured` 系列 | 功能全但依赖重 |
| 图片（OCR 需求） | 需额外处理 | LangChain Loader 不支持 OCR |

### 3.2 什么时候需要后备方案？

```python
# Markdown 加载：主方案 + 后备
try:
    loader = UnstructuredMarkdownLoader(file_path)
    return loader.load()
except ImportError:
    # 后备方案：当 unstructured 库未安装时用纯文本
    loader = TextLoader(file_path, encoding="utf-8")
    return loader.load()
```

---

## 4. 课后作业

1. **添加 HTML 支持**：按照本文档的步骤，为 CaraBot 添加 HTML 文件支持
2. **添加 CSV 支持**：LangChain 有 `CSVLoader`，尝试添加 CSV 格式支持
3. **添加 EPUB 支持**：用 `UnstructuredEPubLoader` 添加 EPUB 电子书支持
4. **批量格式测试**：准备 PDF、DOCX、MD、TXT、HTML 各一份，上传到 CaraBot，验证每种格式的解析效果
5. **设计插件架构**：思考如何将格式支持从硬编码改为插件注册机制（类似 `@register_loader(".pdf")` 装饰器）

---

## 📚 扩展阅读
- LangChain Document Loaders 完整列表：https://python.langchain.com/docs/integrations/document_loaders/
- Unstructured 库：https://github.com/Unstructured-IO/unstructured

---

**下节课预告**：第 24 课 - 多语言支持（跨语言嵌入、语言检测、多语言搜索）
