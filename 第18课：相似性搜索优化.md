# CaraBot Agent 智能知识库系统课程
## 第 18 课：相似性搜索优化

### 🎯 本节课目标
- 理解 RAG 检索的三个核心参数：top_k、threshold、collection
- 掌握搜索性能优化的索引、过滤、分页策略
- 学会分析搜索日志以调优参数
- 学习时长：2 天
- 实践任务：优化搜索性能

---

## 1. 三个核心参数

### 1.1 top_k：返回数量

```python
# top_k=3：只返回最相似的 3 条
results = await search_service.search(query="什么是RAG", top_k=3)

# top_k=20：返回 20 条（给 LLM 更多上下文）
results = await search_service.search(query="什么是RAG", top_k=20)
```

| top_k | 优点 | 缺点 |
|-------|------|------|
| 小（1-3） | 快速，精确 | 可能遗漏相关信息 |
| 中（5-10） | 平衡 → **CaraBot 默认 5** | - |
| 大（20+） | 信息全面 | 慢，噪声多，LLM 上下文窗口压力大 |

### 1.2 threshold：相似度阈值

```python
# threshold=0.9：只返回高度相关的（可能为 0 条）
results = await search(query, threshold=0.9)

# threshold=0.5：宽松匹配（噪声多）
results = await search(query, threshold=0.5)

# threshold=0.7：CaraBot 默认
```

**阈值调优**：
- 太高 → "为什么搜不到？明明文档里有！"
- 太低 → "为什么搜出这些不相关的东西？"
- 建议：从 0.7 开始，根据 badcase 反馈逐步调整

### 1.3 collection：集合过滤

```python
# 按集合隔离不同的知识域
results_hr = await search("请假流程", collection="hr_policies")
results_tech = await search("请假流程", collection="tech_docs")
```

---

## 2. SearchService 实现分析

### 2.1 完整流程

```python
class SearchService:
    async def search(self, query, top_k=None, threshold=None, collection=None):
        # ① 参数默认值
        top_k = top_k or self._settings.top_k                          # 5
        threshold = threshold if threshold is not None else self._settings.similarity_threshold  # 0.7

        start = time.perf_counter()

        # ② 嵌入查询
        query_embedding = await self._embedding_service.embed_query(query)

        # ③ 向量检索
        hits = await self._vector_store.search(
            query_embedding, top_k=top_k, collection=collection, threshold=threshold,
        )

        elapsed_ms = (time.perf_counter() - start) * 1000

        # ④ 格式化结果
        results = [{
            "document_id": doc.metadata.get("document_id", ""),
            "content": doc.page_content,
            "score": round(score, 4),
            "metadata": {k: v for k, v in doc.metadata.items() if k != "document_id"},
        } for doc, score in hits]

        logger.info(
            "Search: query='%s...', top_k=%d, found=%d, time=%.1fms",
            query[:50], top_k, len(results), elapsed_ms,
        )
        return {"query": query, "total_results": len(results),
                "results": results, "search_time_ms": round(elapsed_ms, 2)}
```

### 2.2 耗时分析

```
一次搜索的总耗时 ≈ 嵌入耗时 + 向量检索耗时

嵌入耗时（BGE-m3 on CPU）：50-200ms（取决于查询长度）
  └── 优化：GPU 推理、查询缓存

向量检索耗时：1-50ms（取决于数据量和索引类型）
  └── 优化：更好索引、分区、减少 top_k
```

---

## 3. 搜索优化策略

### 3.1 索引优化

```
FLAT：O(n) 扫描 → 10万数据 ~50ms
IVF_FLAT：O(√n) → 100万数据 ~10ms
HNSW：O(log n) → 100万数据 ~2ms
```

### 3.2 过滤前置

Milvus 支持在向量搜索前先做标量过滤：

```python
# 先过滤条件，再在小范围内做向量搜索
results = col.search(
    data=[query_embedding],
    anns_field="embedding",
    param=search_params,
    limit=top_k,
    expr='doc_id in ["doc1", "doc2"]',  # ← 标量过滤前置
    output_fields=["text", "doc_id"],
)
```

### 3.3 查询缓存

对高频查询缓存嵌入向量：

```python
class CachedEmbeddingService:
    def __init__(self, embedding_service, redis_client):
        self._embedding = embedding_service
        self._redis = redis_client

    async def embed_query(self, text: str) -> list[float]:
        cache_key = f"emb:{hashlib.md5(text.encode()).hexdigest()}"
        cached = await self._redis.get(cache_key)
        if cached:
            return json.loads(cached)

        embedding = await self._embedding.embed_query(text)
        await self._redis.set(cache_key, json.dumps(embedding), ex=3600)  # 1小时过期
        return embedding
```

### 3.4 结果重排序（Re-ranking）

初步检索 top_k=20，再用更强的模型对 20 条重排序，返回 top_k=5：

```python
# 粗排（快速）
candidates = await vector_store.search(query_embedding, top_k=20)

# 精排（准确但慢）
from sentence_transformers import CrossEncoder
reranker = CrossEncoder("BAAI/bge-reranker-large")
pairs = [[query, doc.page_content] for doc, _ in candidates]
scores = reranker.predict(pairs)

# 按新分数排序，取 top-5
reranked = sorted(zip(candidates, scores), key=lambda x: x[1], reverse=True)[:5]
```

---

## 4. 搜索性能监控

### 4.1 关键指标

```python
# 在 SearchService.search 中记录
logger.info(
    "Search: query='%s...', top_k=%d, found=%d, time=%.1fms, "
    "embed_time=%.1fms, vs_time=%.1fms",
    query[:50], top_k, found, total_ms, embed_ms, vs_ms,
)
```

| 指标 | 含义 | 告警阈值 |
|------|------|---------|
| `found` | 返回结果数 | < top_k 的 20%= 几乎没找到 |
| `search_time_ms` | 总耗时 | > 500ms = 用户体验差 |
| `embed_time_ms` | 嵌入耗时 | > 300ms = 考虑 GPU |
| `vs_time_ms` | 向量检索耗时 | > 100ms = 考虑索引优化 |

---

## 5. 课后作业

1. **参数实验**：固定查询，分别测试 top_k=1/5/10/20 和 threshold=0.5/0.7/0.9，观察结果差异
2. **性能基准**：测量 100 次搜索的 P50/P95/P99 耗时
3. **重排序实验**：实现 CrossEncoder 重排序（BGE-reranker），对比有/无重排序的 top-5 精度
4. **缓存实验**：实现查询嵌入的 Redis 缓存，对比缓存命中前后的耗时
5. **过滤优化**：在 Milvus 搜索中加标量过滤条件，对比过滤前置 vs 后置的性能差异

---

## 📚 扩展阅读
- Milvus 索引说明：https://milvus.io/docs/index.md
- Cross-Encoder 重排序：https://www.sbert.net/examples/applications/cross-encoder/

---

**下节课预告**：第 19 课 - RAG 效果评估（Recall@k、badcase 分析、evaluate.py）
