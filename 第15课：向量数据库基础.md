# CaraBot Agent 智能知识库系统课程
## 第 15 课：向量数据库基础

### 🎯 本节课目标
- 理解向量空间模型和相似性计算（余弦相似度、欧氏距离、内积）
- 掌握向量数据库和传统数据库的核心差异
- 了解主流向量数据库（Milvus、Chroma、Pinecone、Qdrant、Weaviate）的定位
- 学习近似最近邻（ANN）检索的基本原理
- 学习时长：2 天
- 实践任务：对比不同向量数据库的查询结果

---

## 1. 向量空间模型

### 1.1 核心思想

把一切（文本、图片、音频）映射到高维向量空间中，语义相似的物体在这个空间中距离更近。

```
文本 "猫"      → 向量 [0.8, 0.3, 0.1, ...]  (1024维)
文本 "小猫"    → 向量 [0.7, 0.4, 0.1, ...]  ← 距离近
文本 "狗"      → 向量 [0.2, 0.7, 0.5, ...]  ← 距离中等
文本 "量子计算" → 向量 [-0.5, -0.8, 0.9, ...] ← 距离远
```

### 1.2 三种相似度度量

| 度量 | 公式 | 范围 | 适用场景 |
|------|------|------|---------|
| **余弦相似度** | `cos(θ) = A·B / (|A|×|B|)` | [-1, 1] | 文本语义（CaraBot 使用） |
| **欧氏距离** | `√(Σ(Ai-Bi)²)` | [0, ∞) | 图像特征、地理位置 |
| **内积（IP）** | `A·B = Σ(Ai×Bi)` | (-∞, ∞) | 归一化后等价于余弦相似度 |

### 1.3 为什么 CaraBot 用内积（IP）？

CaraBot 在生成嵌入时做了 `normalize_embeddings=True`，归一化后所有向量长度 = 1：

```
归一化后：|A| = |B| = 1
cos(θ) = A·B / (1×1) = A·B = 内积
```

内积计算比余弦相似度少一步除法，效率更高。Milvus 和 Chroma 都支持 IP 索引。

---

## 2. 向量数据库 vs 传统数据库

### 2.1 核心差异

| 维度 | 传统数据库（PostgreSQL） | 向量数据库（Milvus） |
|------|------------------------|-------------------|
| 数据形态 | 行和列（结构化） | 高维向量 + 元数据 |
| 查询方式 | `WHERE name = '张三'` | `找到与 query_vector 最相似的 top_k 个向量` |
| 索引类型 | B-Tree、Hash | IVF、HNSW、FLAT |
| 相似性概念 | 无（精确匹配） | 有（语义近似） |
| 典型查询 | `SELECT * FROM docs WHERE status='active'` | `search(query_vec, top_k=5)` |

### 2.2 各自适用的场景

- **传统数据库**：需要精确数据、事务、JOIN → 订单系统、用户系统
- **向量数据库**：需要语义搜索、推荐、相似度排序 → RAG 检索、图片搜索、推荐系统
- **两者配合**（CaraBot 的做法）：PostgreSQL 存元数据 + Milvus/Chroma 存向量

### 2.3 CaraBot 的分工

```
PostgreSQL (documents 表)
  存储：id, filename, file_hash, file_size, author, mime_type, chunk_count, status, created_at...
  查询：按哈希查重、按文件名查找、统计文档数、按时间排序...

Milvus / Chroma (向量存储)
  存储：chunk_id, embedding(1024d), text, doc_id, metadata(JSON)
  查询：语义搜索 → 返回相似 chunks + 分数
```

---

## 3. 主流向量数据库对比

| 数据库 | 类型 | 部署 | 适用 | 特点 |
|--------|------|------|------|------|
| **Milvus** | 分布式 | K8s/Docker | 生产 | 支持十亿级向量，性能最强 |
| **Chroma** | 嵌入式 | 进程内 | 开发/小项目 | 极简部署，pip install 即用 |
| **Pinecone** | 云服务 | SaaS | 不想自己运维 | 全托管，按量付费 |
| **Qdrant** | 混合 | Docker/K8s/嵌入式 | 中等规模 | Rust 编写，性能好 |
| **Weaviate** | 混合 | Docker/K8s | 中等规模 | 内置向量化模块 |
| **pgvector** | PostgreSQL 扩展 | PostgreSQL | 已有 PG 的项目 | 不需额外部署 |

---

## 4. 近似最近邻（ANN）检索

### 4.1 为什么需要 ANN？

**暴力搜索（FLAT）**：将查询向量与数据库中所有向量逐一计算相似度 → O(n×d)，100万条 × 1024维 → 太慢。

**ANN 的思想**：不跟所有向量比，只跟"可能在附近"的那些比 → O(log n)。

### 4.2 索引类型一览

| 索引 | 原理 | 精度 | 速度 | 内存 | 适用 |
|------|------|------|------|------|------|
| **FLAT** | 暴力搜索 | 100% | 慢 | 低 | <10万数据 |
| **IVF_FLAT** | 先聚类再搜索 | 95-99% | 中 | 中 | 10万-1000万 |
| **IVF_PQ** | 聚类+压缩 | 90-95% | 快 | 低 | 千万-亿级 |
| **HNSW** | 图结构 | 98-99% | 很快 | 高 | 百万级，追求精度 |

CaraBot 的 Milvus 实现默认使用 FLAT 索引（`index_type: "FLAT"`），适合中小规模数据或对精度要求极高的场景。生产环境可切换为 HNSW。

---

## 5. Milvus 集合 Schema 设计

### 5.1 CaraBot 的 Schema

```python
fields = [
    FieldSchema(name="id", dtype=DataType.VARCHAR, max_length=128, is_primary=True),
    FieldSchema(name="embedding", dtype=DataType.FLOAT_VECTOR, dim=1024),
    FieldSchema(name="text", dtype=DataType.VARCHAR, max_length=65535),
    FieldSchema(name="doc_id", dtype=DataType.VARCHAR, max_length=128),
    FieldSchema(name="metadata", dtype=DataType.JSON),
]
```

### 5.2 设计要点

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | VARCHAR(128) | chunk 的唯一 ID（UUID） |
| `embedding` | FLOAT_VECTOR(1024) | BGE-m3 生成的向量 |
| `text` | VARCHAR(65535) | 原始文本，返回给用户 |
| `doc_id` | VARCHAR(128) | 关联到 documents 表，用于按文档删除 |
| `metadata` | JSON | 灵活扩展（source, author, file_hash...） |

**为什么 metadata 用 JSON 而不是固定字段？**
因为不同类型的文档可能有不同的元数据。JSON 列可以灵活存储任何结构，而无需修改表结构。

---

## 6. 课后作业

1. **相似度计算**：手算三个简单向量的余弦相似度（用 Python numpy）
2. **数据库选型决策**：为以下场景选择向量数据库并说明理由：
   - 个人笔记 RAG（1000 篇笔记）
   - 企业知识库（10 万文档，1000 用户）
   - 电商图片搜索（1 亿图片）
3. **索引实验**：在 Milvus 中对比 FLAT、IVF_FLAT、HNSW 三种索引的查询速度与召回率
4. **Schema 设计**：为"电商商品搜索"场景设计 Milvus 集合的 Schema（商品名、描述、价格、图片向量...）
5. **传统数据库 vs 向量数据库**：用 PostgreSQL 通过 pgvector 扩展实现向量检索，与传统 SQL 查询对比体验

---

## 📚 扩展阅读
- Milvus 架构：https://milvus.io/docs/architecture_overview.md
- ANN 算法综述：https://arxiv.org/abs/1705.10467
- pgvector：https://github.com/pgvector/pgvector

---

**下节课预告**：第 16 课 - Chroma 向量存储（开发环境）
