# CaraBot Agent 智能知识库系统课程
## 第 6 课：分层架构设计

### 🎯 本节课目标
- 深入理解 CaraBot 的分层架构：API 层、服务层、数据层的职责与协作
- 掌握 FastAPI 依赖注入（Depends）在真实项目中的应用
- 跟踪一次 HTTP 请求在分层架构中的完整处理流程
- 对比 Java Spring Boot 的三层架构，理解 Python 项目的架构差异
- 学习时长：2 天
- 实践任务：分析请求处理流程

---

## 1. 为什么需要分层架构？

### 1.1 没有分层的代码是什么样的？

```python
# ❌ 所有逻辑写在一个文件里
@app.post("/upload")
async def upload(file: UploadFile):
    # 解析文件
    content = await file.read()
    # 计算哈希
    h = hashlib.sha256(content).hexdigest()
    # 查数据库判断重复
    session = get_session()
    result = await session.execute(select(DocumentRecord).where(...))
    # 切块
    chunks = split_text(content)
    # 生成向量
    embeddings = model.encode([c.page_content for c in chunks])
    # 存入向量库
    milvus.insert(embeddings)
    # 存数据库
    session.add(record)
    await session.commit()
    # 返回结果
    return {"ok": True}
```

**问题**：
- 接口函数 200+ 行，难以阅读
- 同样的逻辑（哈希计算、向量检索）在多个接口中复制粘贴
- 改一个环节（比如换向量数据库）要改所有相关接口
- 无法单独测试某个环节

### 1.2 分层后的代码是什么样的？

```python
# ✅ 分层后：接口层只做"接活儿 → 叫服务 → 返回"
@router.post("/upload")
async def upload(
    file: UploadFile,
    upload_service: UploadService = Depends(get_upload_service),
):
    result = await upload_service.upload(
        await file.read(), file.filename, file.content_type,
    )
    return result
```

**好处**：
- 接口函数 < 10 行，一眼看懂
- 所有上传相关的复杂逻辑都在 `UploadService` 里，只写一次
- 换向量数据库只需改 `vectorstore/` 层，服务层不变
- 每一层可以独立测试

---

## 2. CaraBot 的完整分层架构

### 2.1 架构全景图

```
┌─────────────────────────────────────────────────────────┐
│                      HTTP 请求                           │
│  POST /api/v1/chat  |  POST /api/v1/search  |  ...      │
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│              中间件层 (middleware/)                       │
│  RequestLoggingMiddleware → AuthGuard → ErrorHandler     │
│  生成 trace_id    认证 API Key    统一错误格式             │
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│              API 层 (api/routes/)                        │
│  health.py | upload.py | search.py | stats.py | chat.py  │
│  职责：路由分发、参数提取、响应格式化                      │
│  类似 Java 的 @RestController                            │
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│              业务逻辑层 (services/)                       │
│  UploadService | SearchService | AgentService | ...       │
│  职责：核心业务编排、流程控制、跨模块协调                  │
│  类似 Java 的 @Service                                   │
└──────┬──────────────┬──────────────┬────────────────────┘
       ▼              ▼              ▼
┌───────────┐ ┌────────────┐ ┌──────────────┐
│  数据层    │ │  嵌入层     │ │  向量存储层   │
│ (models/) │ │ (embedding) │ │ (vectorstore)│
│ PostgreSQL│ │ BGE-m3     │ │ Milvus/Chroma│
└───────────┘ └────────────┘ └──────────────┘
```

### 2.2 各层职责速查表

| 层级 | 目录 | 职责 | 不能做的事 |
|------|------|------|-----------|
| **中间件** | `middleware/` | 认证、日志、错误处理 | 不能包含业务逻辑 |
| **API 层** | `api/routes/` | 接收请求、参数验证、调用服务、返回响应 | 不能包含业务逻辑 |
| **服务层** | `services/` | 业务编排、流程控制、调用下层 | 不能直接操作 HTTP |
| **数据层** | `models/` | 数据库模型定义、会话管理 | 不能包含业务逻辑 |
| **向量存储层** | `vectorstore/` | 向量增删查 | 不能包含业务规则 |
| **核心层** | `core/` | 配置、日志、异常定义 | 不能依赖其他业务层 |

---

## 3. 依赖注入：分层架构的黏合剂

### 3.1 CaraBot 中的依赖注入全景

在 `main.py` 的 `create_app()` 中，所有服务按依赖顺序创建并注入：

```python
def create_app() -> FastAPI:
    settings = load_settings()                    # ① 加载配置

    db_engine, session_factory = create_engine_and_session(settings)  # ② 数据库

    vector_store = get_vector_store(settings)     # ③ 向量存储（工厂模式）

    embedding_service = EmbeddingService(settings)  # ④ 嵌入模型

    # ⑤ 服务层：按依赖关系逐层构建
    doc_processor = DocumentProcessor(settings)
    dedup_service = DedupService(session_factory)
    upload_service = UploadService(
        settings, doc_processor, embedding_service, vector_store, dedup_service
    )
    search_service = SearchService(settings, embedding_service, vector_store)
    stats_service = StatsService(settings, dedup_service, vector_store)

    # ⑥ Agent 层：依赖上面所有服务
    tools = create_tools(search_service, stats_service, upload_service)
    agent_service = AgentService(settings, tools, search_service, stats_service, upload_service)

    # ⑦ 中间件
    auth_guard = AuthGuard(settings)

    # ⑧ 注册路由，注入服务
    register_routers(app, auth_guard, upload_service, search_service, ...)
```

**创建顺序即依赖顺序**。每个服务只知道自己需要的下层服务，不需要知道整个应用的结构。

### 3.2 依赖关系图

```
settings (配置)
   │
   ├──→ db_engine, session_factory
   │       │
   ├──→ vector_store ←── settings.vector_store_type
   │       │
   ├──→ embedding_service
   │       │
   │       ├──→ doc_processor ←── settings.chunk_size
   │       │
   │       ├──→ dedup_service ←── session_factory
   │       │       │
   │       ├──→ upload_service ←── doc_processor + embedding_service + vector_store + dedup_service
   │       │
   │       ├──→ search_service ←── embedding_service + vector_store
   │       │
   │       ├──→ stats_service ←── dedup_service + vector_store
   │       │
   │       └──→ agent_service ←── tools(search + stats + upload) + settings
   │
   └──→ auth_guard ←── settings.api_key
```

### 3.3 注入方式：为什么 CaraBot 用工厂函数而非 Depends？

CaraBot 使用**手动工厂函数注入**而非 FastAPI 的 `Depends()` 自动注入：

```python
# CaraBot 的方式：手动注入
def create_chat_router(auth_guard: AuthGuard, agent_service: AgentService):
    router = APIRouter()

    @router.post("/chat")
    async def chat(
        body: ChatRequest,
        _api_key: str = Depends(auth_guard),   # 只有 AuthGuard 用 Depends
    ):
        result = await agent_service.run(body.message)
        return result

    return router
```

**为什么？** 因为 CaraBot 的服务是有状态的（`EmbeddingService` 持有模型实例、`AgentService` 持有编译好的图），不能用 `Depends` 每次请求新建。手动注入保证整个应用生命周期内只有一个实例。

---

## 4. 一次完整请求的跟踪

### 4.1 以 POST /api/v1/search 为例

```
1. 客户端发送 POST /api/v1/search
   Body: {"query": "什么是RAG", "top_k": 5, "threshold": 0.7}

2. RequestLoggingMiddleware.dispatch()
   - 生成 trace_id = "a1b2c3d4"
   - 记录请求开始时间
   - request.state.trace_id = trace_id
   - 调用下一个中间件

3. AuthGuard.__call__()
   - 读取 Header: X-API-Key
   - 与 settings.api_key 对比
   - ✅ 通过，继续

4. FastAPI 路由匹配 → search.py 的 search() 函数
   - FastAPI 自动解析 Body → SearchRequest 对象
   - 调用 search_service.search(query, top_k, threshold)

5. SearchService.search()
   ├── 5a. embedding_service.embed_query(query)
   │      → BGE-m3 模型推理 → 返回 1024 维向量
   ├── 5b. vector_store.search(query_embedding, top_k, threshold)
   │      → Milvus/Chroma 向量检索 → 返回 [(doc, score), ...]
   └── 5c. 格式化结果 → 返回 dict

6. API 层将 dict 转成 JSON → 返回给客户端

7. RequestLoggingMiddleware 记录响应：
   "POST /api/v1/search → 200 (45.2ms)"
```

### 4.2 以 POST /api/v1/upload 为例（更复杂）

```
1. 客户端 POST /api/v1/upload
   FormData: file=@doc.pdf, author=张三

2. 中间件（日志 + 认证）

3. upload.py → upload_service.upload(file_content, filename, mime_type, author)

4. UploadService.upload() — 编排整个流水线：
   │
   ├── 4a. 校验文件大小（与 settings.max_file_size 比较）
   │
   ├── 4b. dedup_service.compute_hash(content) → SHA256
   │     dedup_service.check_duplicate(hash)
   │     → 如果是重复文件，直接返回已有记录（短路）
   │
   ├── 4c. dedup_service.find_by_filename(filename)
   │     → 如果是增量更新（同文件名、不同哈希），标记旧记录待删除
   │
   ├── 4d. doc_processor.load_and_split(content, filename)
   │     → 根据 MIME 选择 Loader（PDF→PyPDFLoader, DOCX→Docx2txtLoader...）
   │     → RecursiveCharacterTextSplitter 切块
   │     → 返回 list[Document]
   │
   ├── 4e. embedding_service.embed_documents(texts)
   │     → BGE-m3 批量推理
   │     → 返回 list[list[float]]（每个 1024 维）
   │
   ├── 4f. vector_store.add_documents(chunks, collection, embeddings)
   │     → Milvus/Chroma 存储向量 + 文本 + 元数据
   │
   └── 4g. dedup_service.register_document(...)
         → PostgreSQL 记录文档元数据（id, hash, filename, chunk_count...）

5. 返回 {document_id, filename, chunk_count, is_duplicate}
```

---

## 5. 与 Java Spring Boot 三层架构的对比

### 5.1 术语对照

| 概念 | CaraBot (Python) | Spring Boot (Java) |
|------|-----------------|-------------------|
| 接口层 | `api/routes/` + 路由函数 | `@RestController` |
| 服务层 | `services/` 类 | `@Service` 类 |
| 数据访问 | `models/` + SQLAlchemy | `@Repository` + JPA |
| 依赖注入 | 手动工厂函数 + `Depends()` | `@Autowired` / 构造器注入 |
| 配置 | `core/config.py` + Pydantic Settings | `application.yml` + `@ConfigurationProperties` |
| 中间件 | Starlette Middleware | `javax.servlet.Filter` |
| AOP | 无（Python 无原生 AOP） | `@Aspect` + `@Around` |

### 5.2 代码量对比：同一个功能

**CaraBot — 上传接口（API 层 5 行）**：
```python
@router.post("/upload", response_model=UploadResponse)
async def upload_file(
    file: UploadFile = File(...),
    upload_service: UploadService = Depends(get_upload_service),
) -> UploadResponse:
    return await upload_service.upload(await file.read(), file.filename, file.content_type)
```

**Spring Boot — 上传接口（Controller 层 ~25 行）**：
```java
@RestController
@RequestMapping("/api/v1")
public class UploadController {

    @Autowired
    private UploadService uploadService;

    @PostMapping("/upload")
    public ResponseEntity<UploadResponse> uploadFile(
        @RequestParam("file") MultipartFile file
    ) {
        try {
            UploadResponse response = uploadService.upload(
                file.getBytes(), file.getOriginalFilename(), file.getContentType()
            );
            return ResponseEntity.ok(response);
        } catch (IOException e) {
            return ResponseEntity.status(500).body(null);
        }
    }
}
```

### 5.3 架构差异总结

| 维度 | CaraBot | Spring Boot |
|------|---------|-------------|
| 分层清晰度 | 按目录约定，靠开发者自觉 | 注解强制，编译器检查 |
| 依赖注入 | 手动控制，灵活但需小心 | 框架自动，约定优于配置 |
| 接口定义 | 无显式接口（鸭子类型） | `interface` + `implements` |
| 事务管理 | 手动 `async with session` | `@Transactional` 声明式 |
| 测试难度 | Mock 灵活，但需手动构造 | Mockito 等工具链成熟 |
| 架构图 | 需要开发者自己画 | Spring Boot Actuator 自动生成 beans 图 |

---

## 6. 实践：分析你写的代码属于哪一层

### 6.1 判断标准

- 这行代码在处理 HTTP 请求/响应吗？ → **API 层**
- 这行代码在编排多个步骤、做业务决策吗？ → **服务层**
- 这行代码在读写数据库/向量库吗？ → **数据层/向量存储层**
- 这段逻辑会在多个接口中用到吗？ → **抽到服务层**

### 6.2 CaraBot 的层间通信规则

```
✅ 允许的调用方向（从上到下）：
   中间件 → API 层 → 服务层 → 数据层 / 向量存储层 / 嵌入层

❌ 不允许的调用方向：
   数据层 → 服务层  （底层不该知道上层）
   服务层 → API 层  （业务逻辑不该依赖 HTTP）
   API 层 → 数据层   （跨层调用，绕过业务逻辑）
```

---

## 7. 课后作业

1. **画出调用链**：从 `POST /api/v1/chat` 入手，画出完整的调用链（中间件 → API → AgentService → tools → 下层服务）
2. **分析 create_app()**：阅读 `main.py` 的 `create_app()` 函数，列出所有服务的创建顺序，解释为什么是这个顺序
3. **找反例**：检查代码中是否有跨层调用（比如 API 层直接操作数据库），如果有，提出重构方案
4. **对比练习**：用 Java 画一个 Spring Boot 的等价架构图，标注对应关系
5. **重构练习**：假设要新增一个 "批量上传" 功能，设计各层需要改哪些文件

---

## 8. 常见问题

**Q: 为什么不直接用 FastAPI 的 Depends 做所有注入？**
A: CaraBot 的服务是有状态的（持有模型实例、连接池等），不适合每次请求新建。手动工厂函数保证单例。

**Q: API 层能直接调用 embedding_service 吗？**
A: 不应该。API 层应通过 SearchService/UploadService 来间接使用嵌入服务，否则业务逻辑就散落到了 API 层。

**Q: 怎么判断一个函数应该放在哪个 Service？**
A: 按业务领域划分。文档相关的（上传、解析、去重）→ UploadService；检索相关的 → SearchService；统计相关的 → StatsService；Agent 相关的 → AgentService。

---

## 📚 扩展阅读
- FastAPI 依赖注入：https://fastapi.tiangolo.com/tutorial/dependencies/
- 分层架构模式：https://martinfowler.com/bliki/PresentationDomainDataLayering.html
- Uncle Bob 的 Clean Architecture：https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html

---

**下节课预告**：第 7 课 - 工厂模式与抽象基类（向量存储的抽象与多态实践）
